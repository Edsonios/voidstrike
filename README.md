# VOIDSTRIKE — Warhammer 40,000 10e Simulator

A single-page, two-player hot-seat battle simulator for Warhammer 40,000 (10th edition rules approximation). Muster forces, set up terrain, deploy, and fight a five-phase battle round with interactive dice. Loads every faction and unit from Wahapedia's public data export.

> Unofficial fan tool. Warhammer 40,000 is © Games Workshop. Game data is © Wahapedia; this project is unaffiliated with both.

---

## Files

```
voidstrike/
├── index.html                     ← the app (open this / serve this)
├── app.js                         ← game engine
├── netlify.toml                   ← Netlify config
└── netlify/functions/
    └── wahapedia.js               ← server-side data proxy (avoids CORS)
```

---

## Running it

### Option A — Deploy to Netlify (recommended: one-click data loading works)

The data-fetch "Fetch (auto)" button only works reliably when the site is served by Netlify, because it uses the bundled server function to pull Wahapedia data without CORS problems.

**Drag-and-drop:**
1. Go to https://app.netlify.com/drop
2. Drag the whole `voidstrike` **folder** onto the page (not just index.html).
3. Wait for deploy; open the URL. Done.

**Git (for ongoing edits):**
1. Push this folder to a GitHub repo.
2. In Netlify: "Add new site" → "Import an existing project" → pick the repo.
3. Build command: none. Publish directory: `.` (already set in `netlify.toml`).

### Option B — Open locally (no install)

Double-click `index.html`. Everything works **except** the auto-fetch button (no server). Use the **Upload CSV files** tab instead — see below.

---

## Loading all factions & units

Faction data **loads automatically** for every visitor from a committed snapshot file (`factions-data.json`) in your repo root. Visitors never fetch from Wahapedia and can't touch the fetch controls — they just open the page and all factions are there.

### Admin / maintainer workflow (password-gated)

The fetch, import and export controls live in the **Admin · Data Management** panel on the muster screen, locked behind a password.

- **Default password:** `voidstrike-admin`
- **To change it:** in `app.js` find the line `const ADMIN_HASH=...` and replace the number with the hash of your chosen password. Compute the hash by opening the browser console on your site and running:
  ```js
  (s=>{let h=0;for(let i=0;i<s.length;i++)h=(h*31+s.charCodeAt(i))|0;return h>>>0;})('your-new-password')
  ```
  Paste the resulting number as the new `ADMIN_HASH`, commit, done.

**To refresh the game data** (e.g. after a GW points/dataslate update):
1. Open the Admin panel, enter the password.
2. **Fetch & Load** (or **Re-fetch (update)** to bypass caches). Run it a couple of times if you want to be sure the data is complete and correct.
3. Click **Export Data Bundle** — this downloads `factions-data.json`.
4. Commit that file to your **repo root** (same folder as `index.html`). Netlify redeploys, and every visitor now auto-loads the new data.

> **Security note:** the password is a front-end gate — it keeps casual visitors out of the data controls, but it is *not* strong security (someone technical could bypass it via the page source or console). Don't reuse a sensitive password. For this use case (stopping randoms from triggering fetches) it's perfectly adequate.

> The data files used for a refresh: `Factions`, `Datasheets`, `Datasheets_models`, `Datasheets_wargear` (required), plus `Datasheets_models_cost` (points), `Datasheets_keywords` (keywords), `Datasheets_abilities` + `Abilities` (abilities).

### Data load order
1. `factions-data.json` committed in the repo (the snapshot you exported) — used for all visitors.
2. Browser localStorage (a returning visitor's cached copy) — instant.
3. Built-in Ultramarines + Chaos Space Marines only (if no snapshot exists yet).

---

## Game modes

- **Boarding Actions** (500 pts) — compact fight through a voidship interior with preset walls and hatchways.
- **Open War** (1000 pts) — larger clash on open ground; place your own terrain.
- **Sandbox** (no points limit, custom board size) — choose board dimensions (12–40 × 10–34 cells; 1 cell = 2″) and field any forces you like.

## Flow of a battle

1. **Muster** — pick factions per player, add units (or "Quick Muster").
2. **Terrain Setup** — choose a tool (Wall / Hatchway / Objective / Erase) and click the board. Walls block movement and line of sight; open hatchways let you move and fight through them. Press **Begin Deployment**.
3. **Deployment** — players alternate placing units in their highlighted Entry Zone.
4. **Battle rounds** — each player turn runs five phases:
   - **Command** — auto: +1 CP and Battle-shock tests.
   - **Movement** — click a unit, then a destination. Inside the green ring = Normal move; beyond it you **roll a D6 Advance**.
   - **Shooting** — click a unit to open fire control: pick a weapon (its range highlights on the board), pick a target, **roll the attacks** (hits → wounds → saves resolve in one batch). Fire each weapon separately.
   - **Charge** — click a unit to see legal targets within 12″, pick one, **roll 2D6**.
   - **Fight** — click any unit in Engagement Range, pick its target, **roll melee**. Chargers fight first.

Toggle **show dice rolls** in the right panel to see every die. **Auto-resolve Phase** plays the current phase for you (handy for watching or speeding through).

---

## Notes & limitations

- Weapon **ability tags** are parsed from Wahapedia (Rapid Fire, Sustained Hits, Lethal Hits, Devastating Wounds, Twin-linked, Torrent, Assault, Pistol, Hazardous, Anti-X) and affect the dice math. Unit **abilities** (Core/Faction/Datasheet) are imported and shown on the unit detail panel, but their rules effects are descriptive only — they aren't auto-applied.
- Weapons import at their **base profile**; wargear-option swaps aren't modelled. Squad sizes are inferred from the datasheet's unit composition — adjust with ± in the muster screen if needed.
- Stats, points, keywords and weapon tags come straight from Wahapedia; the rest is a faithful-but-simplified take on the core rules.

---

## Detachments, doctrines, stratagems & enhancements

A faction-agnostic detachment engine lives in `detachments.js`. Each faction is one entry; the file currently includes the full **Space Marines / Adeptus Astartes** set (17 detachments — Gladius, Anvil Siege, Ironstorm, Firestorm, Stormlance, Vanguard Spearhead, 1st Company, Librarius Conclave, Bastion, Orbital Assault, Ceramite Sentinels, Armoured Speartip, Headhunter, plus Ultramarines' Blade of Ultramar & Reclamation Force).

How it works in play:
- **Muster**: pick a Detachment per army from the new dropdown. Chapter-specific detachments only show for that chapter.
- **Command phase**: name your **Oath of Moment** target, and (for detachments with doctrines) select a **Combat Doctrine** / Psychic Discipline. Effects apply army-wide until your next Command phase.
- **Stratagem panel** (battle, left column): shows your **CP** and the stratagems whose timing window is open right now. Click to spend CP; target-self stratagems prompt for a unit. Effects feed into the live dice resolution.
- **Enhancements**: assigned to characters in code (`unit.enh`); their buffs apply to the bearer's unit.

Effects that are mechanically wired into combat include: Oath re-rolls, doctrine eligibility (shoot/charge after Advance/Fall Back), +1 Hit/Wound, granted weapon abilities ([SUSTAINED HITS], [LETHAL HITS], [DEVASTATING WOUNDS], [LANCE], [IGNORES COVER], [ASSAULT], [PRECISION], [ANTI-X]), Armour-of-Contempt AP worsening, -1 to be hit/wounded, Feel No Pain, invulnerable saves, bonus attacks/strength/AP, objective-control bonuses and sticky objectives.

### Adding more factions
Paste a faction's detachment rules, enhancements and stratagems and they're encoded into `DETACHMENT_DATA` using the same schema (see the top of `detachments.js` for the effect-code vocabulary). Currently **Space Marines**, **Black Templars**, **Necrons**, **Blood Angels**, **Dark Angels**, **Space Wolves**, **Deathwatch**, **Grey Knights**, **Orks**, **Tyranids**, **T'au Empire**, **Aeldari**, **Genestealer Cults**, **World Eaters**, **Death Guard**, **Chaos Daemons**, **Thousand Sons**, **Chaos Knights**, **Chaos Space Marines**, **Adeptus Custodes**, **Adeptus Titanicus**, **Adeptus Mechanicus**, **Agents of the Imperium**, **Drukhari**, and **Leagues of Votann** are filled in — every other army (Chaos factions, Orks, Tyranids, Necrons, Aeldari, Drukhari, T'au, Astra Militarum, Custodes, Sisters, Mechanicus, Knights, Agents, Votann, Genestealer Cults, and the divergent SM chapters' own detachments) still needs its data pasted in.

> Detachment rules text is GW's copyrighted material. The data file stores only the **mechanical effects** in brief, original wording — not the published flavour text.


### Factions encoded so far
- **Space Marines / Adeptus Astartes** — Oath of Moment + 17 detachments (incl. Ultramarines' Blade of Ultramar & Reclamation Force).
- **Black Templars** — Templar Vows army rule (replaces Oath of Moment) + 4 supplement detachments (Companions of Vehemence, Vindication Task Force, Godhammer Assault Force, Wrathful Procession). Black Templars also inherit the generic Codex detachments **except** 1st Company Task Force, per the supplement's restrictions. The Wrathful Procession detachment adds selectable **Litanies**; Black Templars use **Vows** instead of Oath.

The engine now supports per-detachment **army-rule overrides** and selectable battle-round states labelled per faction (Doctrine / Vow / Litany / Psychic Discipline).


#### Necrons (added)
The first non-Astartes faction. Reanimation Protocols (army rule) + 9 detachments: Awakened Dynasty, Annihilation Legion, Canoptek Court, Obeisance Phalanx, Hypercrypt Legion, Starshatter Arsenal, Cryptek Conclave, Cursed Legion, Pantheon of Woe.

Wired into combat: Command Protocols (+1 Hit when a CHARACTER leads — modelled as a friendly character within 3"), Cold Fervour (+2 S to Destroyer Cult), Worthy Foes (+1 Wound for Noble/Lychguard/Triarch vs the marked unit), Pantheon's "unravelling" +1 AP aura, plus the usual granted abilities, defensive -1 Hit/Wound, -1 Damage, invuln saves, fight-on-death and re-rolls. New generic engine effects added this pass: `subIncomingStr` (reduce attacker Strength) and `ignoreModifiers` (cancel negative Hit/Wound modifiers) — these also benefit other factions.

Stored & shown but not yet auto-resolved (need subsystems the sim doesn't have): **Reanimation Protocols healing** (a `reanimate` hook is in place and will light up when a repair system exists), **Hyperphasing / Deep Strike / Strategic Reserves** teleporting, and the **Power Matrix / unravelling** spatial states (the combat *effects* of unravelling apply, but the board-state tracking that sets it is pending).


#### Blood Angels (added)
The Sons of Sanguinius chapter — keeps Oath of Moment (unlike Black Templars) but, being on the Oath exclusion list, receives the Hit re-roll WITHOUT the Wound re-roll (handled by the existing codexPure gate). Five supplement detachments: Liberator Assault Group, The Lost Brethren (Death Company), The Angelic Host (jump-pack reserves), Angelic Inheritors (two selectable Angelic Legacies via the Legacy mechanism), and Rage-cursed Onslaught. Blood Angels also inherit the full generic Codex detachment set (including 1st Company Task Force — no restriction). New engine support: the Red Thirst opt-in stratagems (Savage Echoes, Red Rampage, Limb from Limb) are encoded as paired buttons — a base version and a stronger "give in to the Red Thirst" version that self-inflicts Battle-shock (selfBattleshock fx) for the bigger buff. New condition codes: chargedThisTurn (Red Thirst / Maddened Ferocity melee bonus) and selfBattleshocked (Maddened Ferocity's +2-Attacks-if-shocked half). The Death Company "below-strength / below-half" wound re-roll uses the existing selfBelowStrength/selfBelowHalf conditions.


#### Dark Angels (added)
The Unforgiven — keep Oath of Moment but sit on the exclusion list (Hit re-roll, no Wound re-roll, via codexPure). Five supplement detachments: Unforgiven Task Force, Inner Circle Task Force, Company of Hunters, Wrath of the Rock, and Lion's Blade Task Force, plus the full generic Codex set (incl. 1st Company — no restriction). New engine support: DEATHWING and RAVENWING are derived by unit type (isDeathwing/isRavenwing helpers — Terminators/Dreadnoughts/Land Raiders/Repulsors/veteran squads become Deathwing; Mounted and Fly-Vehicles become Ravenwing) so the keyword-gated rules fire whether or not a datasheet spells them out. New condition codes: selfBelowStrength, selfWithinObjective (approximating "within range of your Vowed objective"), deathwing, ravenwing, and targetInfMountedChar. Dark Angels lean heavily on Battle-shock as a POSITIVE trigger (Stubborn Tenacity, Weapons of the First Legion, Pennant of Remembrance, Unforgiven Fury) — all handled via the existing selfBattleshocked condition.


#### Space Wolves (added)
Sons of Russ — keep Oath of Moment but sit on the exclusion list (Hit re-roll, no Wound re-roll). Five detachments: Saga of the Hunter, Saga of the Bold, Saga of the Beastslayer, Champions of Fenris, and Saga of the Great Wolf, plus the full generic Codex set (incl. 1st Company). The supplement's headline mechanic is the **Saga**: most detachment rules have a baseline effect plus an upgraded effect that unlocks once the Saga objective is met. Since the sim can't auto-detect campaign tallies (units destroyed, ground held), there's a **manual "Mark Saga completed" toggle** in the stratagem panel — flip it once you've met the objective and the upgraded effects switch on. Saga of the Great Wolf instead uses **Hunting Packs** (Encircling Jaws / Hunter's Eye / Ferocious Strike), handled through the existing once-per-battle doctrine mechanism. New state: sagaDone per player (reset each battle). New condition codes: sagaCompleted, outnumbersTarget, targetEngagedByAlly, and packsQuarry (the OR of the latter two — Pack's Quarry's trigger). Curse of the Wulfen (a positional Objective-Control aura) is noted but tracked by the players.

**Bug fix this round:** the `rerollOnePerPhase` effect (once-per-phase re-roll of one Hit/Wound/Damage die) was referenced in the data but never wired into combat — meaning Ironstorm Spearhead's *Armoured Wrath* rule and the *Gunnery Honours* enhancement had been silently doing nothing. It's now implemented (approximated as a re-roll of 1s, since a player always re-rolls a failed die), so those effects — and Space Wolves' *Heroes All* — now apply.


#### Deathwatch (added)
Keep Oath of Moment but sit on the exclusion list (Hit re-roll, no Wound re-roll). **Black Spear Task Force is their only detachment** — unlike the other chapters, Deathwatch do NOT get the generic Codex detachments (no merge IIFE), matching the supplement's restriction. The detachment rule is **Mission Tactics** (Furor → [SUSTAINED HITS 1], Malleus → [LETHAL HITS], Purgatus → Critical Hits grant [PRECISION]), handled through the once-per-battle doctrine mechanism with a "Mission Tactic" label. The special-ammunition stratagems are encoded mechanically: Hellfire Rounds → [ANTI-INFANTRY 2+] (via antiVal), Kraken Rounds → +1 AP ranged, Dragonfire Rounds → [ASSAULT]+[IGNORES COVER]. No new condition codes were needed.

The **Kill Teams** army rule (a mixed-Toughness unit wounds on its majority Toughness) is a no-op in this sim: each unit is modelled with a single Toughness value, so there are never multiple Toughness values within one unit to reconcile. It's noted in the detachment's restrictions list rather than faked.

This completes all five divergent loyalist Space Marine chapters with supplements (Black Templars, Blood Angels, Dark Angels, Space Wolves, Deathwatch). Codex-compliant chapters (Ultramarines, Imperial Fists, Salamanders, Blood Ravens, etc.) already work out of the shared Space Marines bucket with full Oath.


### Strategic Reserves / Deep Strike subsystem (NEW — engine feature)
A real off-board reserves system now exists. A reserved unit is removed from the battlefield (it stays alive, so it is never counted as destroyed, but it is excluded from combat, targeting and scoring). At the start of a player's Movement phase the **Reinforcements step** prompts placement: click a cell more than 9″ from all enemy units to set a reserved unit down via Deep Strike, or Skip to hold it back. Units may also be sent into reserves mid-game by stratagems (new `toReserves` fx) or be granted Deep Strike (`grantDeepStrike` fx). The turn a unit arrives, its `setUpThisTurn` flag is set so "just deep-struck" bonuses (re-rolls, etc.) apply, then clears at its next Command phase. The AI/headless resolver auto-places reserves at a valid cell. This activates dozens of previously-dormant Deep-Strike / reserve-recall stratagems across every faction already encoded (Orbital Teleportarium, Guerrilla Tactics, Coordinated Strike, Relic Teleportarium, and more).

**Bug fix this round:** the movement-eligibility effects (`eligShootAfterAdvance`, `eligShootAfterFallBack`, `eligChargeAfterAdvance`, `eligChargeAfterFallBack`) were present in the data across many detachments (Stormlance, Bastion, Company of Hunters, Intractable, etc.) but were never wired into the shoot/charge gates — so those rules silently did nothing. A new `unitHasElig()` helper now resolves them (from army rule, detachment rule, active doctrine, active stratagems and enhancements, honouring conditions) and lifts the Advance/Fall-Back restrictions accordingly.

#### Grey Knights (added)
Their own faction (not an Astartes chapter), psyker-heavy and built entirely around teleportation. Army rule **Gate of Infinity** is backed by the new reserves subsystem — at the end of the opponent's Fight phase a 0CP "Gate" button in the panel pulls eligible Deep-Strike units (scaling 2/3/4 with battle size) into Reserves to redeploy. Six detachments: Brotherhood Strike (Fury of Titan — re-roll 1s the turn a unit arrives), Hallowed Conclave (Terminators act after Falling Back), Banishers (Channelled Force — psychic melee gain Lethal Hits), Sanctic Spearhead (Mailed Fist — vehicles gain Assault on the Advance), Augurium Task Force (prescient redeployment), and Warpbane Task Force (Hallowed Ground — re-roll Hits, full re-roll on your objectives/deployment zone, approximating the consecration-zone mechanic). New condition codes: targetChaos, isTerminator, isVehicle. The positional Hallowed Ground zone is approximated as your held objectives / deployment zone, like other positional auras (Curse of the Wulfen).


### Waaagh! subsystem (NEW — Orks army rule)
A once-per-battle army-wide buff toggle. A new **"Call the WAAAGH!"** button appears in the stratagem panel during an Ork player's Command phase. Calling it sets `waaaghActive` for that player until the start of their next Command phase, at which point it auto-expires (tracked by `waaaghTurnLeft`). While active, the Ork army rule's fx fire (gated on the new `waaaghActive` condition): charge after Advancing (via the eligibility system), +1 Strength and +1 Attack on melee weapons, and a 5+ invulnerable save. The panel shows a green "WAAAGH! ACTIVE" banner while it runs, and "already called" once spent. Many Ork detachment effects and stratagems gate on `waaaghActive` (Bully Boyz' Armed To Da Teef, More Dakka!'s shooting-phase Sustained Hits, etc.). New state: waaaghCalled / waaaghActive / waaaghTurnLeft per player (reset each battle). New condition: waaaghActive.

#### Orks (added)
Eleven detachments: War Horde (Get Stuck In — melee Sustained Hits), Da Big Hunt (Prey-hunting Beast Snaggas, modelled on the Oath-target mechanism), Kult of Speed (Speed Freeks act after Advancing/Falling Back), Dread Mob (Try Dat Button — randomised weapon abilities, modelled as Sustained Hits), Green Tide (Mob Mentality — Boyz invulns), Bully Boyz (second Waaagh! for Nobz), Taktikal Brigade (Blood Axe Taktiks), More Dakka! (Assault ranged, Sustained while a Waaagh! is active), Freebooter Krew (loot-objective Sustained Hits), Speedwaaagh! (Turbo Boostas), and Blitz Brigade (disembark re-rolls). The Waaagh! is the headline mechanic; per-unit Waaagh! stratagems (Get Stuck In Ladz, Grab and Bash) and positional/loot-objective effects are approximated as army-wide or objective-based, consistent with how other positional auras are handled. Mortal-wound, embark, and battle-shock-weapon stratagems remain dormant pending those subsystems.


### Synapse subsystem (NEW — Tyranids army rule)
A *positional* army-wide buff (unlike the Waaagh! toggle). A Tyranids unit is "in Synapse Range" while it is a SYNAPSE model itself or within 6" of a friendly SYNAPSE model — computed live from board position by `unitInSynapse()`. While in Synapse Range: +1 Strength on melee attacks (army-rule fx gated on the new `withinSynapse` condition), and Battle-shock tests are rolled on 3D6 keeping the highest two. Many detachment effects and stratagems gate on `withinSynapse`. **Shadow in the Warp** is a once-per-battle panel button (Command phase) that forces every enemy unit to take a Battle-shock test, −1 within 6" of a Synapse model. The Synaptic Nexus detachment adds **Synaptic Imperatives**: each battle round you pick one of three (Augmentation 5+ invuln / Surging Vitality +1 Advance & Charge / Goaded to Slaughter +1 melee Hit) via panel buttons; each is usable once per battle and applies only to in-Synapse units (conditions `synAug`/`synSurge`/`synGoad`). The Feed-the-Swarm healing (Assimilation Swarm) runs in the Command phase via the reanimation hook: each HARVESTER heals a wounded friendly Tyranids unit within 6". New state: shadowUsed / synapticImp per player. New conditions: withinSynapse, synAug, synSurge, synGoad, targetInfantrySwarm, isHarvester, isTyranidWarrior.

#### Tyranids (added)
Seven detachments: Invasion Fleet (Hyper-adaptations as battle-long doctrines — Sustained vs Infantry, Lethal vs Monsters/Vehicles, Precision vs Characters), Crusher Stampede (Enraged Behemoths — wounded Monsters hit and wound harder), Unending Swarm (Endless Multitude Surge moves, approximated), Assimilation Swarm (Feed the Swarm healing), Vanguard Onslaught (charge after Falling Back / Advancing), Synaptic Nexus (the Synaptic Imperatives), and Warrior Bioform Onslaught (Leader-beasts — Tyranid Warriors get a 5+ invuln). Mortal-wound output, full model-return regeneration via stratagem, and Surge/Tunnel-marker movement remain approximated or dormant pending those subsystems; the combat-shaping core (Synapse buffs, invulns, eligibility, Shadow, Imperatives, Harvester healing) is live.


### Markerlight subsystem (NEW — T'au For the Greater Good)
A shooting-phase, two-role army mechanic. A new **"Mark targets (markerlights)"** button appears in the panel during a T'au player's Shooting phase. Pressing it makes every eligible unit an Observer that marks the closest visible enemy as **Spotted** (each enemy markable once); units with the MARKERLIGHT keyword additionally flag their target for cover-ignoring fire. Any T'au unit then shooting a Spotted enemy is **Guided** — it gets +1 to hit (the new `guided` condition), and against markerlight-Spotted targets the attack also ignores cover (the `guidedML` condition). Spotted marks reset at the start of each Shooting phase. The army-rule fx are gated on `guided`/`guidedML`; the AI/headless resolver auto-marks before shooting. New state: spotted / spottedML per player. New conditions: guided, guidedML, plus battle-round gates round3plus (Kauyon) and round1to3 (Mont'ka), and within9.

#### T'au Empire (added)
Six detachments: Kauyon (Patient Hunter — rounds 3-5 Sustained Hits, and Guided units ignore Hit modifiers vs Spotted targets), Mont'ka (Killing Blow — rounds 1-3 Assault, and Guided units get Lethal Hits), Retaliation Cadre (Bonded Heroes — Battlesuits gain +1 Strength within 12" and +1 AP within 9"), Kroot Hunting Pack (Hunter's Instincts plus a split 6+/5+ melee/ranged invuln), Auxiliary Cadre (Kroot/Vespid support auras, approximated as a flat ranged +1 AP), and Experimental Prototype Cadre (Superior Craftsmanship +6" range, with weapon-upgrade enhancements). The markerlight Guided loop, the battle-round-gated detachment rules, Bonded Heroes' distance bands, and the Kroot dual invuln are all live. Drone upgrades, the +6" range bonus, shaken/mortal-wound stratagems, and several reposition tricks are approximated or dormant pending those subsystems.


### Battle Focus subsystem (NEW — Aeldari army rule)
A per-battle-round **token economy** — the first resource-pool mechanic. At the start of each battle round an Asuryani army receives Battle Focus tokens scaled to battle size (2 / 4 / 6 for 500 / 1000 / 2000 pts), plus +1 for the Warhost detachment's Martial Grace and +1 per Timeless Strategist enhancement. The panel shows the current token count during the Aeldari player's turn with spend buttons for the Agile Manoeuvres: Swift as the Wind (+2", or +3" with Martial Grace), a generic reposition (D6+1"), Star Engines (the selected Vehicle's ranged weapons gain [ASSAULT]), Sudden Strike, and Flitting Shadows (no Overwatch). Spending decrements the pool and, for the movement manoeuvres, raises the unit's effective Move (now folded into the Movement-phase distance checks via a per-unit `_bfMove`). Star Engines sets a per-unit `_bfAssault` flag read by the combat-mod builder. Unspent tokens are wiped and the pool refreshed at the start of the next round (granted in both the post-deployment round-1 hook and the round-increment in endTurn). Per-unit Battle Focus flags clear at the start of each Movement phase.

#### Aeldari (added)
Twelve detachments — the largest roster so far: Warhost (Martial Grace, +1 token), Windrider Host (jetbike reserve recycling), Spirit Conclave (wraith constructs + Vengeful Dead, approximated), Guardian Battlehost (Defend at All Costs — +1 Hit near objectives), Ghosts of the Webway and Serpent's Brood (Harlequins — the latter granting Sustained Hits to Mounted/Vehicle weapons), Devoted of Ynnead (death-fuelled reactions), Seer Council (Strands of Fate; the Fate-dice economy isn't separately tracked but the Stratagems work), Aspect Host (Path of the Warrior as a re-roll-Hits-or-Wounds doctrine pair), Armoured Warhost (Skilled Crews — Vehicle ranged Assault), and the two Corsair detachments (Eldritch Raiders, Corsair Coterie) whose unit-applied Corsair Enhancements are modelled as regular enhancements. New conditions: objectiveContested, isHarleMountedVeh, strLowerThanT. The token economy, the move-allowance integration, Star Engines, Skilled Crews, the objective- and reserve-based detachment rules, and the various invulns are live; the many D6+1" reposition manoeuvres log intent and raise effective Move but (like all extra-move effects) don't physically reposition the AI token, and mortal-wound / Fate-dice / return-to-life effects remain approximated or dormant.


### Cult Ambush subsystem (NEW — Genestealer Cults army rule)
A resource-pool-fed resurrection-into-reserves mechanic that reuses the existing Strategic Reserves / Deep Strike machinery. At battle start a GSC army receives Resurgence points by battle size (6/10/14 for 500/1000/2000 pts, +2 per Deeds That Speak to the Masses enhancement). When an eligible GENESTEALER CULTS unit (non-Character, non-Vehicle) is destroyed, a death-hook in resolveAttacks automatically spends the appropriate number of Resurgence points (scaled to the unit's model count by `resurgenceCost`) to recreate an identical full-strength unit and place it into Cult Ambush — modelled as Strategic Reserves (`inReserve`, off-board) so it arrives in a later Movement phase through the same Deep Strike flow the Grey Knights and others use. Arrival bonuses (Host of Ascension's Sustained + Ignores Cover, A Chink in Their Armour's Lethal Hits) fire via the existing `setUpThisTurn` flag. The panel shows the live Resurgence pool during the GSC player's turn. New state: resurgence per player. New conditions: isMountedVehicle, within18, within6OfAlly.

#### Genestealer Cults (added)
Six detachments: Host of Ascension (A Perfect Ambush — reinforcements gain Sustained + Ignores Cover), Xenocreed Congregation (Unquestioning Fanaticism — Character-led units re-roll Advance/Charge), Biosanctic Broodsurge (Hypermorphic Fury — +1 Charge and a post-charge +1 Attack), Outlander Claw (Rapid Takeover — Mounted/Vehicle +1 OC), and the two allied-faction detachments Brood Brother Auxilia (Astra Militarum allies) and Final Day (Tyranids allies). The Brood Brother / Final Day army-composition rules (including allied units and the Astra Militarum overlapping-fire and Tyranid Catalyst auras) are not modelled — those auras are approximated as +1 Hit via the Oath-target mechanism or a within-6-of-ally check — but all their Stratagems work. The Resurgence economy, the death→reserves→arrival chain, the arrival bonuses, and the combat-shaping detachment rules are live; the literal Cult Ambush marker geometry, mortal-wound stratagems, and per-model healing remain approximated or dormant.


### Blessings of Khorne subsystem (NEW — World Eaters army rule)
A dice-pool gambling mechanic — the seventh distinct army-mechanic shape. At the start of each battle round a World Eaters army can make a Blessings of Khorne roll: eight D6 (rollKhorneBlessings), then spend doubles/triples to activate up to two of the six Blessings for the round. Each Blessing requires a double (or triple) of a threshold value or higher — Unbridled Bloodlust (1), Rage-fuelled Invigoration (2), Total Carnage (3), Martial Excellence (4), Warp Blades (5), Decapitating Strikes (6) — and khorneAvailable scans the current dice to see which are satisfiable, while activateKhorneBlessing consumes two qualifying dice and caps activation at two per round. The panel shows the live roll, the activatable Blessings (with their dice requirement), and which are active. The three melee Blessings (Martial Excellence → Sustained Hits 1, Warp Blades → Lethal Hits, Decapitating Strikes → Devastating vs INFANTRY) are injected army-wide by the combat-mod builder; the other three gate via the khBlood/khRage/khCarnage conditions. All Blessing state (active list, dice, rolled flag) is wiped and re-rollable at the start of the next battle round. New state: khorneBlessings, khorneDice, khorneRolled per player. New conditions: khBlood, khRage, khCarnage, khMartial, khWarp, khDecap.

#### World Eaters (added)
The first Chaos faction. Six detachments: Berzerker Warband (Relentless Rage — a unit that charged gets +1 Attack and +2 Strength in melee), Cult of Blood (Idols of Khorne as a per-Command-phase aura doctrine — Infinite Rage / Burning Wrath / Blessed Blood, approximated army-wide while chosen), Khorne Daemonkin (Blood Tithe — a separate point economy plus Blood Legions allies), Possessed Slaughterband (Brazen Fury reactive move), Goretrack Onslaught (Rush to the Fray — disembarking units get +1 Charge and Lance), and Vessels of Wrath (Wrath of Khorne — Vessel-of-Wrath marking plus an extra Blessing). The Blessings dice-pool, the melee-Blessing grants, the Relentless Rage / Rush to the Fray disembark and charge rules, and the Idol auras are live. The Blood Tithe economy and Blood Legions allied composition (Khorne Daemonkin) are not modelled — that detachment's Stratagems still work — and the Vessel-of-Wrath marking, Brazen/Blood-Surge reactive moves, mortal-wound output, and fight-on-death (stored via fightOnDeath) remain approximated or dormant.


### Nurgle's Gift / Contagion subsystem (NEW — Death Guard army rule)
A positional debuff-aura — the eighth distinct army-mechanic shape, and the first that debuffs ENEMIES by proximity rather than buffing friendlies. Contagion Range grows over the battle (contagionRange: 3" round 1, 6" round 2, 9" round 3+). unitAfflictedBy(enemyUnit, dgPlayer) tests whether an enemy is within Contagion Range of any on-board Death Guard model (or in the per-round force-Afflicted list dgAfflictExtra used by Miasmic Bombardment / stratagems). An Afflicted enemy suffers two things: -1 Toughness against all Death Guard attacks (folded into the wound-target calc via an effective toughness), plus the army's single chosen Plague (selected in the panel, persists all battle, changeable each round by Champions of Contagion / Manifold Maladies): Skullsquirm Blight (-1 to the Afflicted unit's own Hit rolls, applied as subIncomingHit when an Afflicted unit attacks), Rattlejoint Ague (worsen the Afflicted unit's Save by 1, via a new worsenSave mod folded into the save calc), or Scabrous Soulrot (worse Move/Ld/OC — characteristic-level, no combat-mod). New state: dgPlague, dgAfflictExtra per player. New condition: targetAfflicted (gates Creeping Blight, Eye of Affliction, Insectile Murmuration). New mods field: worsenSave.

#### Death Guard (added)
Seven detachments: Virulent Vectorium (Worldblight — sticky objectives that project Nurgle's Gift), Mortarion's Hammer (Miasmic Bombardment — round-start force-Affliction of distant units), Champions of Contagion (Manifold Maladies — swap your Plague each round), Tallyband Summoners (Plague Legions daemon allies), Shamblerot Vectorium (Numberless Horde — Poxwalker reinforcement waves), Death Lord's Chosen (Deadly Vectors — mortal wounds to Afflicted units in the opponent's Command phase), and Flyblown Host (Verminous Haze — Scouts + Stealth, the latter approximated as a -1-to-Hit-at-range). The Contagion aura, the -1 Toughness, the Skullsquirm/Rattlejoint Plagues, the targetAfflicted-keyed detachment effects, and the sticky-objective rule are live. The Plague Legions allied composition (Tallyband Summoners), the Poxwalker reinforcement spawning (Shamblerot), Deadly Vectors' and the many stratagems' mortal-wound output, Miasmic Bombardment's automatic target selection, and Scabrous Soulrot's characteristic penalties remain approximated or dormant.


### Shadow of Chaos subsystem (NEW — Chaos Daemons army rule)
A zone-control mechanic tied to a Battle-shock economy — the ninth distinct army-mechanic shape. zoneOfCell classifies every board cell as player 1's deployment zone, player 2's, or No Man's Land (derived from entryZones). inShadowOfChaos(hx,hy,daemonPlayer) returns true for the daemon player's own deployment zone (always), for No Man's Land or the enemy zone while they control at least half that zone's objectives (objectiveControlInZone tallies summed OC within range), and for 6" bubbles around Corrupted objectives (the Corrupt Realspace stratagem). The two army-rule effects are wired into the real Command-phase Battle-shock test: Daemonic Manifestation (a daemon in its own Shadow that passes its test heals D3 via reanimateUnit) and Daemonic Terror (an enemy in the daemon player's Shadow, or within 6" of a Greater Daemon, takes -1 to its test and D3 mortal wounds on a failure, applied via a new applyMortals helper). Tzeentch's Fates in Flux is a second mechanic: a shared re-roll-token pool (Scintillating Legion starts with 3, the panel shows the count, spending one passes it to the opponent, and you draw one back in your Command phase if they hold any). New state: fluxTokens, shadowCorrupt per player. New conditions: inShadow/selfInShadow, targetBattleshocked, kwKhorne/kwTzeentch/kwNurgle/kwSlaanesh. New helpers: zoneOfCell, objectiveControlInZone, ocOf, inShadowOfChaos, nearGreaterDaemon, applyMortals, grantFlux/spendFlux.

#### Chaos Daemons (added)
Six detachments: Daemonic Incursion (Warp Rifts deep-strike + the four mono-god relic enhancements), Shadow Legion (Be'lakor's per-god boons — Khorne charge-after-Advance, Tzeentch -1 to be hit, Nurgle -1 to be wounded), and the four mono-god legions Blood (Khorne — sticky objectives, Lance, fight-on-death), Excess (Slaanesh — charge after Falling Back, +Hit/+Wound auras), Plague (Nurgle — 9" Shadow projection, Battle-shock pressure), and Scintillating (Tzeentch — Fates in Flux). The zone model, the Manifestation/Terror Battle-shock effects, the Flux token economy, and the per-god combat conditions are all live. The mono-god army-composition restrictions and the Daemonic Pact / Blood Legions allied rules are not modelled (the sim runs a single-faction army); the many Battle-shock-test stratagems, mortal-wound stratagems, reactive Surge moves, and the on-demand Flux re-roll (granted and tracked, but the dice resolver doesn't pause mid-roll for it) remain approximated or dormant. Note this build also activated the engine's first real mortal-wound output path (applyMortals), previously dormant.


### Cabal of Sorcerers subsystem (NEW — Thousand Sons army rule)
A psychic roll-against-difficulty system — the tenth distinct army-mechanic shape. At the start of your Shooting phase, each sorcerer can attempt one Ritual via the panel: attemptRitual rolls 2D6 (with an optional Channel for a third die), applies Perils (D3 mortal wounds via applyMortals) if any double/triple was rolled, and manifests the Ritual if the total meets its Warp Charge. The four Rituals (each manifestable once per turn, tracked in ritualsAttempted): Destiny's Ruin (WC5 — marks an enemy for re-roll Hits of 1, or all Hits at 10+), Temporal Surge (WC6 — a friendly unit repositions, logged), Doombolt (WC7 — D3 mortal wounds, D3+3 at 11+), and Twist of Fate (WC9 — marks an enemy for +1 AP, +2 at 12+). The marked-target buffs (ritualTargets, cleared each Shooting phase like T'au's Spotted) are injected army-wide by the combat-mod builder keyed on the target unit. New state: ritualTargets, ritualsAttempted, kindredTSon per player. New helpers: RITUALS, attemptRitual, manifestRitual, closestEnemyToAny, isThousandSonsArmy, isPsykerUnit. New conditions: ritDestiny/ritDestiny10/ritTwist/ritTwist12, inFlow, kindredMaelstrom, psychicOnly. Note the Daemons zone model was generalised into a faction-agnostic inControlZone helper so the Hexwarp Thrallband's Flow of Magic reuses it directly.

#### Thousand Sons (added)
Six detachments: Grand Coven (Kindred Sorcery — a per-Command-phase psychic ability choice), Changehost of Deceit (Scintillating Legions daemon allies), Warpmeld Pact (Tzaangor mutant-sacrifice mechanics), Rubricae Phalanx (All is Dust resilience), Warpforged Cabal (vehicle-sorcery synergy), and Hexwarp Thrallband (Flow of Magic — the same zone model as the Daemons' Shadow of Chaos, granting +1 psychic Wound while in your zone). The Ritual mechanic, the marked-target buffs, Doombolt's mortal wounds, Kindred Sorcery's Psychic Maelstrom, and the Flow of Magic zone are all live. The Scintillating Legions allied composition (Changehost) and the Warpmeld Sacrifice backlash are not modelled; the many psychic-range / characteristic-level enhancements, the Hazardous/Perils-on-the-attacker stratagems, and Temporal Surge's reposition remain approximated or logged. Psychic-weapon detection is approximate (a `psychic` flag or name match) since the built-in templates don't tag weapon abilities granularly.


### Harbingers of Dread subsystem (NEW — Chaos Knights army rule)
An escalating-menu mechanic — the eleventh distinct army-mechanic shape, and the first that grows over the course of the battle. Deathly Terror is active from the start (seeded at the post-deployment battle-start alongside the other army-rule grants), and at the start of rounds 1, 3 and 5 the Chaos Knights player banks one more Dread ability via a Command-phase panel. Banked abilities are permanent and never repeat, so the army's effects accumulate as the game goes on. Built as stacking flags (dreadActive per player) rather than a per-round refresh — closest in spirit to the Khorne Blessings flag set, but additive and permanent. The seven abilities split across the engine: Deathly Terror and Despair worsen an enemy unit's effective Leadership in the Battle-shock test (folded into the test as effLd = ld + dreadLdPenalty, which sums the active Ld auras when a Chaos Knights model is within aura range); Doom (+1 Wound vs Battle-shocked) and Darkness (-1 to be hit by Battle-shocked or >18" attackers) are injected into the combat-mod builder; Delirium adds D3 mortal wounds (via applyMortals) when a below-half enemy near a Knight fails its Battle-shock test; Dismay adds Battle-shock tests; and Dominion widens the aura range from 9" to 12" (dreadAuraRange, read by every aura). New state: dreadActive, dreadRolledRound per player. New helpers: DREAD_ABILITIES, dreadHas, dreadName, seedDread, bankDread, dreadAuraRange, dreadLdPenalty, isChaosKnightsArmy. New condition: dreadDoom. This build also generalised army-wide objControl: a new armyOcBonus helper folds detachment-rule objControl fx (scope:'army', e.g. Lords of Dread's +2 OC to Knight Characters) into the scoring OC calculation, honouring the fx condition per unit — previously objControl only worked when committed by a stratagem.

#### Chaos Knights (added)
Six detachments: Traitoris Lance (Paragons of Terror — bank an extra Dread ability at round 1), Infernal Lance (Malefic Surge — modelled as a doctrine-style army pick: Unholy Hunger / Diabolic Power [Lethal or Sustained] / Unnatural Fortitude [5+ invuln or 6+ FNP]), Lords of Dread (Tyrannical Court — +2 OC to Knight Characters, now live in scoring), Helhunt Lance (Masters of the Pack — War Dog aura-sharing), Houndpack Lance (Marked Prey — War Dog Sustained vs a marked target, plus the Battleline/Character army composition), and Iconoclast Fiefdom (Dreaded Masters — DAMNED allies and Dark Sacrifice). The Dread accumulation, the Battle-shock auras, Doom/Darkness/Delirium, the Malefic Surge picks, and the Lords of Dread OC are all live. The Dreadblades allied Knights and the Super-heavy Walker movement rule are army-composition / movement rules not modelled; the DAMNED allied composition (Iconoclast), the War Dog aura-sharing (Helhunt/Masters of the Pack), Malefic Surge's Leadership-test backlash, and the various Deadly-Demise / Battle-shock-on-the-enemy stratagems remain approximated or logged.


### Dark Pacts subsystem (NEW — Chaos Space Marines army rule)
A risk/reward weapon-boon mechanic that turned out to be a near-twin of the Chaos Knights' Malefic Surge built the previous turn. Each Shooting/Fight phase a unit can make a Dark Pact via the panel: take a Leadership test (fail → D3 mortal wounds via applyMortals), then weapons gain [LETHAL HITS] + [SUSTAINED HITS 1] until end of phase. Modelled as a per-phase army-wide toggle (darkPact per player, cleared at every phase start) rather than a per-unit token. New helpers: isHereticAstartesArmy, isRenegadeWarband, makeDarkPact, plus BILE_AUGMENTS/isCreationsOfBile and haTerrorActive for two detachments. New conditions: pactActive, pactMeleeKhorne, pactRangedTzeentch, pactRangedNurgle, pactMeleeSlaanesh, pactUndivided (the Pactbound Zealots Mark-of-Chaos crit/reroll riders, gated on the active pact and the unit's Mark + weapon type), plus attackerBattleshocked (Nightmare Hunt). Three reuses stood out: Focus of Hatred (Veterans) and Vendetta (Renegade Warband) both "mark one enemy each Command phase for Hit re-rolls" — identical to Oath of Moment, so they reuse the oathTarget marker and the targetIsOath condition rather than new state; the two terror detachments (Dread Talons, Nightmare Hunt) reuse the Battle-shock step (a -1-to-the-test penalty wired through haTerrorActive); and Creations of Bile reuses the escalating-augment-menu shape from Harbingers of Dread (bileAugments, a battle-start chooser). The Renegade Warband's Slaves to None correctly suppresses the Dark Pact panel (they trade pacts for army-wide [ASSAULT]).

#### Chaos Space Marines (added)
Fifteen detachments — the largest faction so far: Veterans of the Long War (Focus of Hatred), Deceptors (Infiltrators), Renegade Raiders (Assault + objective AP), Dread Talons (terror), Fellhammer Siege-host (Iron Fortitude — conditional Wound penalty when S>T), Pactbound Zealots (Marks of Chaos — pact-gated crit riders), Chaos Cult (Desperate Devotion), Soulforged Warpack (Vashtorr vehicle pacts), Cabal of Chaos (Empyric Wellspring), Creations of Bile (Experimental Augmentations), Nightmare Hunt (terror + below-half/Battle-shock combat bonuses), Huron's Marauders (Tyrannical Motivation), Renegade Warband (Slaves to None + Vendetta), Warpstrike Champions (Warp Portals reserves), and Cult of the Arkifane (Soul Forge Boons — vehicle 5+ invuln). The Dark Pact mechanic, the Mark-of-Chaos riders, the terror Battle-shock penalties, Iron Fortitude, the Vendetta/Focus marks, and the Arkifane vehicle invuln are all live. The Cult of the Dark Gods cross-faction allies (Berzerkers/Rubricae/Plague/Noise Marines), the DAMNED Desperate-Pact movement bonuses, the Soul-Link/datasheet-copy and many CP-economy enhancements, and a number of mortal-wound / Battle-shock-on-the-enemy stratagems remain approximated or logged.


### Generic round-pick stance matcher (NEW — engine, enables Custodes / AdMech / Deathwatch tactics)
The biggest single piece of leverage this turn was an engine change, not a faction. Many of the remaining Imperial codices share one shape: "at the start of the round (or Command phase), choose option A or B; it applies army-wide until your next turn." The Space Marine doctrine machinery already modelled this (`activeDoctrine` + a per-id `doctrine:<name>` condition), but each new stance previously needed its own hard-coded condition case. Added a single generic fallthrough so `doctrine:<anything>` resolves against the active pick — meaning Custodes Martial Ka'tah, AdMech Doctrina Imperatives, Custodes Shield Host Martial Mastery, Deathwatch Mission Tactics, Drukhari Combat Drugs and Sororitas Vows now all slot in with zero new condition code. An army-wide round-pick with no detachment doctrines is encoded via `armyRule.choices`/`choiceLabel` (already wired through `doctrineSource`); a detachment that adds its own second pick uses its `doctrines` list (which correctly takes precedence). Also added three conditions: `selfAlone` (no friendly units within 6"), `isWalker`, and `selfAtStartingStrength`.

#### Adeptus Custodes (added)
Six detachments. The army rule Martial Ka'tah is an on-fight stance pick: Dacatarai (melee [SUSTAINED HITS 1]) or Rendax (melee [LETHAL HITS]), routed through `armyRule.choices` and the new generic matcher. Talons of the Emperor (Null/Deadly aura, approximated), Shield Host (Martial Mastery — a second round-pick as a detachment doctrine list: melee crit-5+ or +1 AP), Null Maiden Vigil (Creeping Dread Battle-shock aura + anti-Psyker work), Auric Champions (Assemblage of Might — mark one enemy each Command phase for +1 Wound from Custodes Characters, reusing the Oath-target marker), Solar Spearhead (Auric Armour — full-strength vehicles get +2 OC via `armyOcBonus`, below-strength re-roll Hits, below-half re-roll Hits+Wounds), and Lions of the Emperor (Against All Odds — a lone unit with no friends within 6" gets +1 Hit/+1 Wound, via `selfAlone`). The test confirmed the lone-unit detection correctly distinguishes a model standing apart from one near an ally, and that the vehicle OC and re-roll thresholds switch at exactly full / below / below-half strength.

#### Adeptus Titanicus (added)
A deliberately minimal stub. Towering Example skips the detachment step entirely and lets a non-Character Titan be Warlord; the only mechanically-modelled army-wide effect is Super-heavy Walker movement, which the AI already abstracts. Encoded as a single placeholder detachment so the faction resolves cleanly in selectors and regression.

**Roster: 21 of 28.** This was the front half of the final batch. Still pending from the eight-codex paste (each needs a new or reused subsystem): Emperor's Children (Pledges-to-the-Dark-Prince predict-kills resource + Thrill Seekers), Drukhari (Pain-token resource pool), Leagues of Votann (Yield-Point pool with a 7-point ability-flip), Adepta Sororitas (Miracle-dice substitute-a-valued-die pool), Adeptus Mechanicus (Doctrina Imperatives — slots straight into the new round-pick matcher), Astra Militarum (Voice of Command Orders — per-unit buff issuance), and Imperial Agents (Deathwatch Mission Tactics — also the round-pick matcher) and Imperial Knights (Code Chivalric Oath = Deed+Quality+Honoured, reusing the Chaos Knights Super-heavy Walker work). These will land over the next turns.


#### Adeptus Mechanicus (added)
Seven detachments, and the first faction to fully validate the generic round-pick matcher built last turn. The army rule Doctrina Imperatives is a round-pick routed through `armyRule.choices`: Protector (ranged [HEAVY], +1 to ranged Hit modelling the BS improvement, and -1 to be hit in melee for Battleline units) or Conqueror (ranged [ASSAULT], +1 to melee Hit, and +1 AP for Battleline). The "BATTLELINE and/or near Battleline" rider is approximated by the unit's own BATTLELINE keyword (AdMech lists are Battleline-heavy, and per-target proximity to a Battleline ally is expensive to evaluate). Two detachments lean directly on the stance pick: Eradication Cohort's Murderous Imperative re-rolls Hits of 1 under Protector and Wounds of 1 under Conqueror (gated on the same `doctrine:` conditions); Cohort Cybernetica and Haloscreed both offer Stratagems to run both Imperatives at once. Data-Psalm Conclave's Benedictions and Haloscreed's Noospheric Overrides are encoded as detachment doctrine-lists (their own second pick). Explorator Maniple's Acquisition objective re-roll reuses objective-proximity conditions, and Cohort Cybernetica's Cyber-Psalm +1 vehicle OC flows through `armyOcBonus`. Rad-Zone Corps (Rad-bombardment), the various mortal-wound and Battle-shock Stratagems, and a damage-reroll stratagem are logged or approximated. The test confirmed Protector and Conqueror grant exactly the right weapon-type-gated bonuses and that Murderous Imperative flips its re-roll cleanly when the stance changes. Added one condition, `targetWithinHalf` (approximated as within 12", since per-weapon range bands aren't tracked).

**Roster: 22 of 28.** Still pending from the eight-codex paste: Emperor's Children (Pledges predict-kills resource + Thrill Seekers), Drukhari (Pain-token pool), Leagues of Votann (Yield-Point pool with a 7-point ability-flip), Adepta Sororitas (Miracle-dice substitute-a-die pool), Astra Militarum (Voice of Command Orders), and Imperial Agents (Deathwatch Mission Tactics — slots into the round-pick matcher) and Imperial Knights (Code Chivalric Oath = Deed+Quality+Honoured). Imperial Agents is the next quick one (matcher reuse); the resource-pool and bespoke-shape factions follow.


#### Agents of the Imperium (added)
Five detachments. The army rule Assigned Agents is chiefly an allied-attachment / Kill-Team build rule, so in a single-faction sim it carries no combat fx — a pure-Agents army just selects a detachment, which is what is encoded. Ordo Xenos uses Deathwatch Mission Tactics, a detachment-specific round-pick (its own doctrine list, routed through the generic matcher): Furor ([SUSTAINED HITS 1]), Malleus ([LETHAL HITS]) or Purgatus ([PRECISION], a slight over-grant since the codex limits it to Critical Wounds). The one new condition this turn was `targetDaemon`, added for Ordo Malleus's Destroy the Daemonic — the test confirmed it correctly fires the bonus Wound re-roll against a Daemon target while the unconditional Hit re-roll applies to any enemy, distinguishing a Daemon unit from a non-Daemon Chaos unit (which is why it earns a dedicated condition rather than folding into `targetChaos`). Ordo Hereticus's anti-Chaos riders (ignores-cover + Sustained, with the "5+ models" clause approximated to any Chaos target) and Imperialis Fleet's Eliminate-at-all-Costs mark (reusing the Oath-target marker for +1 Hit) both validated cleanly. The Officio Assassinorum Extremis abilities (Veiled Blade), the various mortal-wound / Battle-shock / shoot-back Stratagems, and the allied-attachment composition are logged or out of scope for the single-faction simulator.

**Roster: 23 of 28.** Remaining from the eight-codex paste, all needing a new or reused resource/bespoke subsystem: Drukhari (Pain-token pool), Leagues of Votann (Yield-Point pool with a 7-point ability-flip), Emperor's Children (Pledges predict-kills resource + Thrill Seekers), Adepta Sororitas (Miracle-dice substitute-a-die pool), Astra Militarum (Voice of Command Orders), and Imperial Knights (Code Chivalric Oath = Deed+Quality+Honoured, reusing the Chaos Knights Super-heavy Walker work). The "fast" matcher-reuse factions are now all done; the resource-pool factions (Drukhari, Votann) come next, then the bespoke shapes.


### Pain tokens (NEW — Drukhari army rule, resource-pool subsystem)
Power from Pain is a resource pool built on the same shape as Aeldari Battle Focus, but with accumulation across the whole game (not a per-round refresh) and three gain triggers wired into existing hooks: +1 at the start of each Command phase (in `doCommandPhase`), +1 each time an enemy unit is destroyed (a new `painOnEnemyDeath` helper called at both death sites alongside `cultAmbushResurrect`), and +1 each time an enemy fails a Battle-shock test (in the Battle-shock loop, granting the opponent's player a token). Spending 1 token Empowers the selected unit for the phase via the panel — turning on its Pain abilities, which are unit-specific datasheet abilities the engine doesn't model individually, so Empowered is abstracted as the unit's weapons gaining [SUSTAINED HITS 1] for the phase (the common offensive Pain effect across the faction). State: `painTokens` (persistent pool), `empowered` (per-unit phase flag, cleared each phase start). New helpers: `isDrukhariArmy`, `gainPain`, `painOnEnemyDeath`, `empowerUnit`, `unitEmpowered`, `allianceOfAgony`; new conditions `empowered`/`selfEmpowered`. Realspace Raiders' Alliance of Agony seeds a flat 6 tokens at battle start (the per-combo accounting isn't tracked), wired at the same post-deployment point as the other army-rule grants. The test confirmed all three gain triggers, that Empower spends exactly one token and boosts only the chosen unit, and that the pool persists and is spent correctly across a full game.

#### Drukhari (added)
Six detachments. Realspace Raiders (Alliance of Agony starting pool), Skysplinter Assault (Rain of Cruelty — disembark ignores-cover + lance, approximated army-wide since transports are abstracted), Spectacle of Spite (Combat Drugs — a Command-phase menu pick encoded as a detachment doctrine-list of WYCH CULT characteristic buffs), Covenite Coterie (Stitchflesh — the S>T −1-Wound defensive, validated), Kabalite Cartel (Murderous Agenda — name a Contract unit for [PRECISION], reusing the Oath-target marker, with a Contract-completion Pain bonus), and Reaper's Wager (Callous Competition — Drukhari start winning the wager for a Hit-1 re-roll; with Harlequin allies unmodelled they stay winning). The Harlequins/Anhrathe allied composition, the various Pain-spending stratagems (Postmortality resurrection, Insensible to Pain, etc.), and transport-embarked interactions are logged or out of scope for the single-faction simulator.

**Roster: 28 of 28 — COMPLETE.** Every Warhammer 40,000 10th-edition faction is now encoded. The natural next milestone is the ability-fx-join layer (the unbuilt half of roadmap step 2): the importer already fetches and displays each datasheet's Core/Faction/Datasheet/Wargear ability TEXT, and the planned join adds a hand-authored ability-name → fx map (keyed by datasheet_id + normalised ability name, kept as its own section so it survives a re-fetch) so those abilities apply mechanical effects instead of only displaying. The enabler is to EXPORT factions-data.json from the live app after a fetch and upload it here, so mappings are authored against the real ability names/text. First consumer: replacing Drukhari's blanket Empowered [SUSTAINED HITS 1] with each unit's actual mapped Pain ability.

---

## PLANNED (roadmap step 2, second half): datasheet-ability → fx join layer

Context: the original 10-step request (step 2) was "Include Faction Abilities, Core Abilities, Model Abilities — fetchable from Wahapedia." The **fetch half is already built** — the importer pulls `Datasheets_abilities.csv` + `Abilities.csv` alongside models/weapons/keywords/cost/wargear, resolves Core/Faction abilities via the shared Abilities table by `ability_id`, takes Datasheet/Wargear abilities inline, and shows them on the unit detail panel. The **unbuilt half is application**: that ability text is display-only; its rules effects are not auto-applied. (This is why, e.g., Drukhari Empowered is a blanket [SUSTAINED HITS 1] stand-in rather than each unit's own Pain ability — `TEMPLATES` carry no per-unit named abilities.)

Planned design (a join key, not a merge):
- Keep fetched ability *text* as the display/reference layer, keyed by `datasheet_id + ability name (normalised)`.
- Add a hand-authored **ability→fx map** keyed by that same stable key, each entry pointing at an `fx` list (the same vocabulary the detachment engine already uses).
- The map is a **sibling layer that must survive a re-fetch** (fetched data is overwritten on balance updates; the map is not). In the exported `factions-data.json` it lives as a separate top-level section the re-fetch preserves; in code it can equally be its own file. Join happens at load time; a missing/renamed ability degrades to "show text, apply nothing" rather than mis-applying.
- Consumers read the unit's mapped fx instead of generic abstractions. First intended consumer: Drukhari `empowerUnit()` reads the selected unit's actual Pain ability (display) and applies its mapped fx (mechanics), replacing the blanket Sustained Hits.

Enabler: the user can EXPORT `factions-data.json` from the live app (after a fetch) and UPLOAD it here, so the fx map can be authored against the real ability names/text/`parameter` values rather than guessed. Division of labour is unchanged: the engine + fx map are Claude's; the ability text lives in the fetched runtime data, never reproduced in Claude's output. This is a substantial, ongoing authoring effort (thousands of datasheet abilities) — build the join plumbing once, prove it on Drukhari Pain, then expand coverage faction by faction. Sequencing: finish the four remaining factions on the hand-fx track first, then build this layer as its own step.


### Yield Points & army-stance flip (NEW — Leagues of Votann army rule)
Prioritised Efficiency is a resource pool built on the Pain-token shape, with a distinctive threshold-driven **state flip**. State: `yieldPoints` (persistent pool), `votannStance` ∈ hostile/fortify per player, plus `assailed`/`pinned` markers for Persecution Prospect. YP are gained automatically at the **end of each Command phase** from objective control — +1 for holding an objective in your own deployment zone, then from round 2 up to +3 more for the escalating not-in-zone conditions (one held outside your zone, two or more held outside, and more than the opponent) — computed via the existing `objectiveControlInZone`/`zoneOfCell` helpers (`votannCommandYield`). At that same point the stance is re-evaluated against the 7YP threshold (`updateVotannStance`): under 7 the army has **Hostile Acquisition** (+1 Hit vs a target on an objective, re-roll Advance/Charge); at 7+ it has **Fortify Takeover** (+1 Hit when your unit is on a held objective, and −1 to an incoming Wound roll when the attack's Strength exceeds the unit's Toughness and it isn't a Vehicle). Crucially the flip only happens at end of Command phase, so spending YP mid-turn (the panel's 3YP manual Toggle, modelling Adaptable Avarice / Mercenary) doesn't auto-revert until the next check — the Designer's-Note subtlety, which falls out for free. New helpers: `isVotannArmy`, `gainYP`, `ypOnEnemyDeath` (Needgaârd Martial Leverage: +1 YP per enemy destroyed, wired at both death sites), `votannCommandYield`, `votannStanceFor`, `updateVotannStance`, `votannFlipStance`, `unitAssailedBy`; new conditions `votannHostile`, `votannFortify`, `targetAssailed`, `selfOnObjective`, `isCthonianBeserks`, `isEinhyr`, and two correctly-sided defensive conditions `incomingStrHigherThanSelfT` + `selfNotVehicle` (these read the *defending* unit's Toughness/keyword in the att/def-swapped defensive fold, which the older shared `strHigherThanT` did not). The test confirmed the escalating YP gain by round, the 7YP flip, both stances' combat effects with correct objective/Strength gating, and the manual flip.

#### Leagues of Votann (added)
Seven detachments. Needgaârd Oathband (Martial Leverage — +1 YP per kill), Persecution Prospect (Assailed From Every Angle — a target-marking debuff with assailed-keyed re-rolls/Wound bonuses; the pin Move/Charge penalty is logged), Dêlve Assault Shift (Fury From The Dêlve — Cthonian Beserks gain Deep Strike + Battleline), Brandfast Oathband (Mobile Sensor Relays — transport-aura Sustained Hits, approximated army-wide since transports are abstracted), Hearthfyre Arsenal (Optimal Application — bonus YP for not-in-zone objectives held near an Iron-master/Memnyr Strategist, wired into the Command-phase yield), Hearthband (Methodical Annihilation — closest-target Wound re-roll + Einhyr AP), and Mercenary Oathband (Ruthless Reinvestment — no auto-flip; manual 3YP stance switch). YP-spend stratagem upgrades (the "spend YP for a bigger effect" riders), the assailed *pinned* state, transports/embark, mortal-wound stratagems, and YP-cost enhancements are approximated or logged, consistent with the rest of the simulator.

**Roster: 25 of 28.** Remaining, each AWAITING the user's pasted rules before encoding: Emperor's Children (Thrill Seekers + a predict-kills Pledge resource), Adepta Sororitas (Miracle-dice substitute-a-die pool), and Astra Militarum (Voice of Command Orders), plus Imperial Knights (Code Chivalric Oath = Deed+Quality+Honoured, reusing the Chaos Knights Super-heavy Walker work).


### Yield Points (NEW — Leagues of Votann army rule, threshold-flip resource pool)
Prioritised Efficiency is a resource pool built on the Pain-token shape, but with a distinctive twist: the pool's *level* flips the whole army between two ability sets rather than being spent on activations. State: `yieldPoints` (persistent pool) and `votannStance` ∈ `hostile`/`fortify`. At the end of each Command phase (`votannCommandYield`), the army gains +1 YP for holding an objective in its own deployment zone, and from round 2 onward +1 each for holding an objective outside its zone, holding two or more outside, and holding more objectives than the opponent — reusing the `objectiveControlInZone`/`zoneOfCell` geometry built for the Daemons' Shadow of Chaos. Then `updateVotannStance` re-evaluates the threshold: ≥7 YP → Fortify Takeover, else Hostile Acquisition. **Hostile** (`votannHostile` condition) gives +1 Hit against enemies near any objective plus Advance/Charge re-rolls; **Fortify** (`votannFortify`) gives +1 Hit when the firing unit itself holds an objective, plus a defensive −1 to incoming Wound rolls when the attack's Strength exceeds the unit's Toughness and it isn't a Vehicle (a new `incomingStrHigherThanSelfT` defensive condition). The panel shows the YP count and current stance, with a Toggle button that spends 3 YP for a manual flip (Mercenary Oathband's Ruthless Reinvestment and Persecution Prospect's Adaptable Avarice). New helpers: `isVotannArmy`, `gainYP`, `ypOnEnemyDeath`, `votannStanceFor`, `votannCommandYield`, `objectiveControlledBy`, `updateVotannStance`, `votannFlipStance`, `unitAssailedBy`. The test confirmed the YP tally per objective condition, Martial Leverage's per-kill gain, both stances' combat effects gated correctly, and the flip firing at exactly the 7-YP threshold.

#### Leagues of Votann (added)
Seven detachments. Needgaârd Oathband (Martial Leverage — +1 YP per enemy destroyed, via the same death hook as Pain/Cult Ambush), Persecution Prospect (Assailed From Every Angle — a shoot-and-label mechanic with an `assailed` marker and a `pinned` Move/Charge debuff; `targetAssailed` condition), Dêlve Assault Shift (Cthonian Beserks gain Deep Strike + Battleline), Brandfast Oathband (Firebase Control transport aura → [SUSTAINED HITS 1], approximated army-wide since transports are abstracted), Hearthfyre Arsenal (Optimal Application — up to +2 YP for outer objectives held with an IRON-MASTER/MEMNYR STRATEGIST, plus a spend-1-YP Hit-1 re-roll), Hearthband (Methodical Annihilation — closest-target/in-engagement Wound-1 re-roll and +1 AP for KÂHL/EINHYR/ÛTHAR), and Mercenary Oathband (Ruthless Reinvestment — no auto-flip; manual 3-YP stance toggle). The many YP-spend stratagem upgrades, Strategic-Reserves repositioning (Nomad Strategist, Hidden Accessways), transport embark/disembark (Secure Positions, New Horizons), mortal-wound output (Wall of Steel, Delayed-Fire Rounds), and the suppression debuffs are stored/approximated or dormant pending those subsystems. The YP economy, the stance flip, both stances' combat effects, and the assailed/pinned labels are live.


### Pledges to the Dark Prince + Favoured Champions (NEW — Emperor's Children)
Emperor's Children layer three mechanics on top of mostly-existing machinery. **Thrill Seekers** (army rule) is the act-after-Advance/Fall-Back eligibility built for Grey Knights/Orks — all four `elig*` fx, army-scoped (the target restrictions aren't enforced by the abstract board). The one genuinely new subsystem is **Pledges to the Dark Prince** (Coterie of the Conceited): at the start of each battle round the player pledges a kill-count (`ecMakePledge`; auto-1 in headless/AI, raised interactively via the panel's Pledge +1 / the Unbound Arrogance stratagem); a death hook (`ecOnEnemyDeath`, wired alongside the Pain/YP kill hooks) tallies kills; and at end of round `ecResolvePledge` either banks that many **Pact points** (kills ≥ pledge) or inflicts D3 mortal wounds on the Warlord via `applyMortals`. Accumulated Pact points gate four cumulative combat tiers in `gatherCombatMods`: 1+ re-roll Hit 1, 3+ re-roll Wound 1, 5+ melee [LETHAL HITS]+[SUSTAINED HITS 1], 7+ Critical Hit on unmodified 5+ (`critOn=5`). **Favoured Champions** (Slaanesh's Chosen, Internal Rivalries) reuses the single-unit marker shape: `favouredChampions` starts on the Warlord's unit (`ecSeedFavoured`) and is reassigned by `ecOnEnemyDeath` to any EC CHARACTER that destroys an enemy unit; the Favoured unit re-rolls the Wound roll. **Daemonic Empowerment** (Carnival of Excess) reuses the Drukhari Empowered pattern — applied army-wide to EC units (Legions of Excess allies aren't modelled) granting [SUSTAINED HITS 1], or crit-on-5+ where already Sustained. New helpers: `isECArmy`, `isCoterieArmy`, `isSlaaneshChosenArmy`, `ecMakePledge`, `ecResolvePledge`, `ecRaisePledge`, `ecPactTier`, `ecSeedFavoured`, `isFavouredChampions`, `ecOnEnemyDeath`. The test confirmed each Pact tier, the pledge bank-vs-Warlord-mortal branch, Favoured re-roll and reassignment, Daemonic Empowerment, Sensational Performance's charge +1 S/AP, and the S>T defensive enhancements.

#### Emperor's Children (added)
Seven detachments. Mercurial Host (Quicksilver Grace — Advance re-rolls; Steeped in Suffering / Intoxicating Musk enhancements), Peerless Bladesmen (Exquisite Swordsmanship — charge-conditional Lethal+Sustained; Death Ecstasy fight-on-death), Rapid Evisceration (Mechanised Murder — Hit-1/Wound-1 re-rolls for VEHICLEs, since disembarking is abstracted), Carnival of Excess (Daemonic Empowerment), Coterie of the Conceited (Pledges), Slaanesh's Chosen (Favoured Champions; Slayer of Champions Precision + anti-CHARACTER buff), and Court of the Phoenician (Sensational Performance — charge +1 S/AP; Close-Quarters Excruciation within-12 +1 S/AP). Thrill Seekers' target restrictions, the Legions of Excess allied composition, the transport embark/disembark and Strategic-Reserves stratagems, the mortal-wound and Battle-shock-test stratagems, and the various Surge/Stimulus reposition moves are stored/approximated or dormant pending those subsystems. Thrill Seekers eligibility, the Pact-point economy and tiers, Favoured Champions, Daemonic Empowerment, Sensational Performance, and the defensive enhancements are live.


### Acts of Faith — Miracle dice (NEW — Adepta Sororitas, substitute-a-die pool)
The thirteenth army-mechanic shape, and the first **dice-substitution** pool — distinct from the resource pools (spend a count) and the dice-pool gamble (read a roll). State: `miracleDice` is an array of pre-rolled *values* per player. You gain one each battle round (`grantMiracleRound`, fired at battle start and in `endTurn`) and one each time one of your own units is destroyed (`miracleOnDeath`, wired at the kill sites alongside the Pain/YP/EC death hooks); each gained die is rolled 1–6 and kept at that value. An Act of Faith substitutes a kept die into a roll. Threading substitution into the batch attack resolver (Hit/Wound/Damage/Advance/Charge/Save) isn't practical — the engine doesn't pause mid-roll — so the **honest, fully-wired** application is **Battle-shock**: the panel's Arm toggle (`sororToggleMiracleBshock`) sets a per-turn flag, and `doCommandPhase` substitutes the best Miracle die (`spendBestMiracle`) for one of the test's 2D6, removing it from the pool. The rest of the economy (round/death gains, valued dice, the pool display) is live; other Acts are resolved against the abstract dice engine and logged. Two detachments add round-pick layers reusing the doctrine/marker shapes: **Penitent Host** Vows of Atonement (`sororVow`, panel pick; Absolution in Battle flows via the `vowAbsolution` condition) and **Champions of Faith** Righteous units (`sororRighteous`, a Command-phase pick of up to 3 via the panel; buffs flow via the `selfRighteous` condition, lasting until the player's next Command phase). New helpers: `isSororitasArmy`, `gainMiracle`, `grantMiracleRound`, `miracleOnDeath`, `spendBestMiracle`, `sororToggleMiracleBshock`, `sororVowFor`, `chooseSororVow`, `isRighteous`, `toggleRighteous`. New conditions: `within6`, `selfRighteous`, `vowAbsolution`. The test confirmed round/death gains (and that non-Sororitas deaths grant nothing), best-die spend, the Battle-shock substitution consuming a die and passing the test, Blood of Martyrs' below-strength/half bonuses, Fervent Purgation's Assault + within-6 Strength, Righteous melee buffs, and the Absolution vow.

#### Adepta Sororitas (added)
Six detachments. Hallowed Martyrs (The Blood of Martyrs — +1 Hit below strength, +1 Wound below half; Spirit of the Martyr fight-on-death), Penitent Host (Desperate for Redemption Vows), Bringers of Flame (Fervent Purgation — ranged [ASSAULT] + within-6 +1 S; Cleansing Flames Torrent [DEVASTATING WOUNDS]), Army of Faith (Sacred Rites — two Acts/phase, abstracted; Light of the Emperor ignore-modifiers), Champions of Faith (Righteous Purpose), and the shared core. The many Miracle-pool-manipulation enhancements/stratagems (re-roll a die, set to 6, convert two into one, gain on a kill/Battle-shock), the Deadly-Demise auto-mortals, transport disembark-and-shoot, resurrection (Divine Intervention), and shoot-back/Surge reactions are stored/approximated or dormant pending those subsystems. The Miracle economy + Battle-shock substitution, the Vow and Righteous round-picks, and the combat-shaping detachment rules are live.


### Voice of Command — Orders (NEW — Astra Militarum, the final faction)
The fourteenth and last army-mechanic shape: a per-unit **issuance** system, distinct from the markerlight producer/consumer (which marks enemies) — here an Officer grants one of six buffs to a friendly unit within range. State: `unitOrders` maps unit id → active Order id; `ordersIssued` tracks the Command-phase count. The six Orders and their wiring: Move! Move! Move! (+3" Move via `_orderMove`, folded into the effective-Move and Advance-total reads), Take Aim! (+1 BS → `m.addHit` on ranged), Fix Bayonets! (+1 WS → `m.addHit` on melee), First Rank Fire! (+1 ranged Attack via the new `plusAttacksRanged` mod + fx, wired into the per-model ranged attack count), Take Cover! (+1 Save → defensive `worsenSave -1`, clamped so no save beats 3+), and Duty and Honour! (+1 OC via `_orderOc` folded into `ocOf`/scoring, +1 Ld via `_orderLd` lowering the Battle-shock target). Orders are issued in the Command phase by `issueOrder` (range/Battle-shock/own-army checks), expire at the start of the issuing player's next Command phase (`clearOrders`), and immediately fall off any unit that becomes Battle-shocked. `autoIssueOrders` drives the AI/headless path (each officer buffs nearby non-officer units up to the budget). `orderBudgetFor` is one Order per Officer, +1 per Officer under Grizzled Company's Ruthless Discipline and +1 per Grand Strategist; `orderRangeFor` is 6" (12" with a Laud Hailer). New helpers: `isAstraArmy`, `ORDERS`, `orderName`, `isOfficer`, `unitOrder`, `applyOrderPersistent`, `issueOrder`, `orderRangeFor`, `orderBudgetFor`, `clearOrders`, `orderCombatMods`, `orderDefMods`, `autoIssueOrders`. New engine plumbing reused by detachments: the `plusAttacksRanged` mod + fx case, and a general `worsenSave` fx case (defensive save modifier, used by Masters of Camouflage). The test confirmed range/shock/own-army gating on issuance, all six Order effects, expiry on Battle-shock, the budget (1 vs Grizzled's 2), Born Soldiers' army-wide ranged Lethal, Grizzled's ordered-unit Hit-1 re-roll, and Masters of Camouflage's capped save improvement.

#### Astra Militarum (added)
Nine detachments. Combined Arms (Born Soldiers — ranged [LETHAL HITS]), Siege Regiment (Artillery Support barrages, abstracted), Mechanised Assault (Armoured Fist — disembark +1 Wound, applied army-wide), Hammer of the Emperor (Iron Tread — Squadron Advance), Recon Element (Masters of Camouflage — +1 Save), Bridgehead Strike (Only the Best — Infantry ranged Hit-1 re-roll), Grizzled Company (Ruthless Discipline — +1 Order/Officer and ordered-unit Hit-1 re-roll), Steel Hammer (Ceaseless Cannonade — shoot into Engagement Range), and Armoured Infantry (Squadron Command). The officer-specific Order counts, Order-targeting and transport/embark/Strategic-Reserves stratagems, super-heavy/TITANIC CHARACTER selection, artillery-barrage board effects, and Deep Strike repositioning are abstracted or dormant pending those subsystems; the Orders themselves, the budget/range modifiers, and the combat-shaping detachment rules are live.

### Roster complete
All 28 factions are encoded across fourteen distinct army-mechanic shapes: toggle (Orks), positional buff/debuff (Tyranids Synapse, Death Guard Contagion), doctrine round-pick (Space Marines), producer/consumer (T'au Markerlights), resource pool (Aeldari Battle Focus, Drukhari Pain, Votann Yield Points), resource-pool resurrection (Genestealer Cults), dice pool (World Eaters Khorne), zone control (Daemons/Thousand Sons), psychic ritual (Thousand Sons Cabal), escalating menu (Chaos Knights Dread), risk/reward toggle (Chaos Space Marines Dark Pacts), characteristic chooser (Creations of Bile), predict-and-deliver wager (Emperor's Children Pledges), dice-substitution pool (Adepta Sororitas Miracle dice), and per-unit issuance (Astra Militarum Orders). The next milestone is the ability-fx-join layer described above.


### Ability → fx join layer (NEW — roadmap step 2, the unbuilt half, now built)
The importer always FETCHED and DISPLAYED each datasheet's Core/Faction/Datasheet/Wargear ability TEXT (stored on every template's `abilities` array as {name,type,desc}, shown on the unit detail panel). What was missing was APPLICATION. This adds it as a JOIN, not a merge.

**The map** lives in its own file, `ability_fx.js` (a new file — upload it to the repo via GitHub "Add file → Upload files", and it must load before app.js; index.html now has `<script src="ability_fx.js">` between detachments.js and app.js). `ABILITY_FX` is keyed by datasheet_id (the TEMPLATES key, e.g. `dru_archon`) → normalised ability name (lowercase alphanumerics) → a list of fx using the **same vocabulary** the detachment engine consumes. It is deliberately a separate code file so it **survives a re-fetch**: factions-data.json is overwritten wholesale on every Wahapedia balance update, but this hand-authored map is not touched.

**The join** is `abilityFxFor(unit, filterFn)` in app.js: it looks up `ABILITY_FX[unit.tpl]`, matches each of the unit's named abilities by normalised name, and returns the concatenated fx. It is **tolerant** — an ability that is renamed or dropped upstream simply misses the lookup and contributes nothing, so the text still displays and nothing crashes (graceful degradation, verified: `abilityFxFor(null)` and an unknown tpl both return `[]`). A new display-only `note` fx (a no-op in applyFxList) records effects the abstract board can't model (return models, reposition to Reserves, suppress, weapon-keyword grants) so the intent is logged without attempting anything illegal. The unit detail panel now tags an ability with a small "✦ engine" marker when it has a mapped, non-note fx, so you can see which displayed abilities actually do something mechanically.

**First consumer — Drukhari Pain abilities.** `empoweredFxFor(unit)` returns the unit's "(Pain)"-tagged ability fx, and the Drukhari Empowered combat injection now applies *that* through the normal applyFxList path instead of the old blanket `[SUSTAINED HITS 1]`. An unmapped/custom Drukhari unit still falls back to the Sustained default, so imported units always do something sensible. `unitHasElig` also consults the join, so a unit whose Pain ability grants eligibility (e.g. Wyches' charge after Advancing/Falling Back) actually gains it when Empowered. Authored against the real export: all 30 Drukhari datasheets that have a Pain ability are mapped (0 unmapped) — 21 to a concrete combat fx (Incubi → melee [DEVASTATING WOUNDS]; Lelith → +1 S/AP melee; Kabalites → re-roll Wound 1; Hand of the Archon → [LETHAL HITS]+[PRECISION]; Reaper → [SUSTAINED HITS 2] ranged; etc.), 9 to logged notes. The test confirmed each unit's Empowered mods now reflect its actual ability (not blanket Sustained), the note no-op, the unmapped fallback, the not-empowered baseline, and the elig grant.

**Extending the map** to other factions is now purely additive: add `datasheet_id → { abilityname: [fx] }` entries to ability_fx.js (any unit not listed is unaffected). The natural follow-ups are the universal Core abilities (Feel No Pain and the stat-derived ones are already read from stat-blocks; Deep Strike/Scouts/Infiltrators/Fights First/Lone Operative could be authored once and apply across hundreds of datasheets) and other factions' signature Datasheet abilities. To author against live data, EXPORT factions-data.json after a fetch and map against the real ability names/text.


### Target-keyword riders (correction) + the per-model-composition caveat
A correction to the join layer's first pass. Target-keyword riders like "only vs INFANTRY" or "only vs VEHICLES" ARE modellable — they're a condition on the target's keywords, checked exactly like the engine's existing `antiKw`/`isVehicle`/`targetCharacter` logic. Three conditions were added — `targetInfantry`, `targetVehicle`, `targetMonster` (each reading `def.allKw`) — and the riders that the first pass had wrongly relaxed were restored: Incubi's Empowered [DEVASTATING WOUNDS] now applies only vs INFANTRY, and the Reaper's Empowered [SUSTAINED HITS 2] only vs VEHICLES. The test confirmed each fires against the correct target type and is withheld against the wrong one. (The earlier "relaxed" note on these was a convenience shortcut mislabelled as a limitation — fixed.)

**Caveat — per-model wargear composition is genuinely not modelled, for two compounding reasons.** First, the engine tracks each unit as a single token with a shared weapon list and a `models` count; it does not track which individual model carries which weapon. Second — and this is why a per-model-token rebuild would not fix it — the imported data does not contain per-model loadouts either: a unit lists every weapon *any* of its models could take as one flat menu (Kabalite Warriors, for instance, list blast pistol, blaster, dark lance, shredder, splinter cannon, splinter pistol and splinter rifle together, with no record of which model carries which or how many). So riders that depend on a unit *containing a specific model type* — Court of the Archon's "if it contains a Lhamaean", Beastmaster's "if it contains a Beastmaster" — cannot be checked from this data and are deliberately relaxed (the fx applies unconditionally) with an in-file comment marking each. This is distinct from the target-keyword riders, which are enforced. Modelling these composition riders would require a data source that carries per-model loadouts in the first place, not just an engine change.


### Core abilities — first batch (CORE_FX)
The "authored once, applies across hundreds of datasheets" batch. A second map, `CORE_FX` in ability_fx.js, is keyed by normalised ability name (not datasheet_id) and applies to ANY unit whose `abilities` array contains a Core ability of that name, faction-agnostic. `coreFxFor(unit)` is the consumer; the defender's Core fx are folded into `gatherCombatMods` through the normal defend path.

**Applied for real:**
- **Stealth** → defender-side −1 to incoming ranged Hit rolls (per the Core Rules; live on 80 real datasheets). Verified to fire vs ranged and not melee.
- **Lone Operative** → enforced as a ranged-targeting restriction in engine code (`unitIsLoneOp`), not as an fx: a Lone Operative unit can only be selected as a ranged target from within 12". Wired into both the interactive shooting target filter and the AI target picker (live on 52 datasheets — assassins, snipers, lone characters). Verified to allow targeting within 12" and block beyond it.

**Deliberately not double-encoded** (handled elsewhere): Feel No Pain (read from the stat-block / FNP path) and Leader (army construction, no battle fx).

**Noted as dormant** (logged via `note` fx, applies nothing — honest placeholders that become real with no schema change once the underlying system/data exists):
- **Deadly Demise** — CAVEAT: the death-hook mechanism exists, but the per-unit magnitude (1 / D3 / D6 ...) is NOT in the export: all 721 entries share one generic glossary string, so the value is unknown and applying a guess would be inventing rules. Noted, not applied.
- **Deep Strike, Scouts, Infiltrators** — deployment/Reserves abilities; the Reserves/deployment system is dormant (and the Scouts X" value isn't in the data either).
- **Firing Deck, Hover** — transport / aircraft mechanics, both dormant.

**Left for a later engine iteration:** Fights First. The Fight phase currently iterates eligible units without a Fights-First ordering step (it only logs the label for chargers), so honouring the ability properly means restructuring the Fight-phase resolution loop — an engine change with regression surface across all 28 factions, deferred rather than half-done. The unit panel's "✦ engine" tag now also marks applied Core abilities (Stealth) and Lone Operative.


### Fights First — Fight-phase two-step restructure
The Fight phase now follows the Core Rules' two-step structure instead of the old "chargers then alternate the rest" shortcut. The resolution loop runs `fightStep(unitFightsFirst)` then `fightStep(not unitFightsFirst)`:
- **Step 1 (Fights First):** units that made a Charge move this turn OR have the Fights First Core ability OR have a granted Fights First effect.
- **Step 2 (Remaining Combats):** everyone else.

In each step, players **alternate** selecting one eligible unit, **starting with the player whose turn is not taking place** (`starter = nonActive`, flipping each pick). Eligibility — alive, in Engagement Range or charged, has a melee weapon, hasn't fought — is **re-checked every iteration**, so a unit that becomes eligible mid-step is picked up (the rules' "previously ineligible units... can then be selected" clause). No unit fights twice (`u.fought` gate); a guard counter caps any runaway loop.

New code: `unitFightsFirst(u)` (charge / Core ability / granted), `unitHasFightsFirstAbility(u)`, and a declarative `fightsFirst` fx case in `applyFxList` (no combat-math effect — it's detected via `unitHasElig`). Eleven previously-dormant grants in detachments.js were wired to emit the `fightsFirst` fx: Sacred Rage, Speed of the Primarch, Master of the Red Thirst, Instinctive Defence, Infernal Infusion, Temporcopia, Chronoshard, Blinding Speed (enhancements, `selfOnly`); Rabid Infusion and Bringers of Despair (stratagems); and Blade of Celerity (appended to its existing assault fx). Two army-rule descriptions where Fights First is conditional or traded away — Strength from Death, Beguiling Aura — were left as prose.

Verified: a Fights-First unit strikes before a normal enemy even when non-active; a charger fights in step 1; with both sides Fights-First the non-active player strikes first via the alternation; a unit granted Fights First by an enhancement is detected and strikes first; two normal units each fight exactly once with no stall; and a full Fight phase driven through the real `autoResolvePhase` completes and advances to the next phase cleanly.

**Honest gap (pre-existing, not introduced here):** the in-combat *movement* sub-sequence — Pile In and Consolidate (the 3" moves) — is not simulated; units fight from where they sit on the hex board. The re-eligibility machinery is live and correct, but the specific trigger described in the rules' note (a Consolidation move bringing new units into Engagement Range) rarely fires here because those moves are abstracted.


### In-combat movement — Pile In & Consolidate
Bridges the gap noted in the Fights-First section. The Fight sequence is now Pile In → make attacks → Consolidate, in both the AI loop (`fightStep`) and the interactive path (`beginFightSelection` piles in on selection; `rollFight` consolidates after attacks, each logged).

Because a unit is a single token (one `{hx,hy}` + a `models` count, no per-model positions), these model the **net unit-level move** rather than per-model choreography:
- **`pileInMove(u)`** — only if engaged; steps up to 3" toward the closest enemy (`closestEnemyTo`, which ignores AIRCRAFT unless the mover can FLY), tightening toward base-to-base (adjacent cell). Never ends out of engagement.
- **`consolidateMove(u)`** — steps up to 3" toward the closest enemy and keeps the move only if it ends in engagement; otherwise falls back to moving toward the closest objective, keeping that move only if it ends within range (≤1.5 cells). Lets a unit that destroyed its foe advance onto a new enemy or seize an objective.
- **`stepToward(u,tx,ty,maxInch)`** — the shared mover: one cell at a time (2" orthogonal, ~2.83" diagonal), greedily reducing distance, never stacking (`cellOccupied`), never crossing blocked terrain (`segBlocked`), never leaving the board, stopping when adjacent to the goal or the 3" budget is spent.

The payoff is that the rules' dynamic re-eligibility now genuinely fires: a unit that wipes its target **consolidates into a fresh enemy and becomes fightable in the same phase** (verified — a killer destroyed one unit, consolidated onto the vacated cell, engaged a second enemy, and fought it). Verified behaviours: Pile In tightens a diagonally-engaged unit to clean adjacency; Consolidate seizes an objective when no enemy is in reach; Consolidate correctly **declines** to move toward an enemy more than 3" away (it would end out of engagement); movers respect blocking models and the board edge; and an 80-phase-step game with positions mutating every Fight phase runs with no stall or crash.

**Still abstracted (token model, not introduced here):** per-model positions, Unit Coherency as a multi-model constraint, spreading a unit's attacks across several engaged enemy units, and which individual models are in base-to-base contact. Those require per-model positions the engine doesn't carry. What's now modelled is the unit-level repositioning that Pile In and Consolidate produce — which is what makes Consolidate-into-new-combat and Consolidate-onto-objective work.


### Equivalence harness — the de-risking tool for the per-model migration
`equiv_harness.js` + `equiv_baseline.json` are test tooling (NOT shipped to Netlify) that pin the engine's current observable behaviour as a deterministic oracle, so the planned per-model rewrite can be proven to change nothing that works today. This is the safety mechanism that makes a foundational rewrite safe by *rigour* rather than by being small: build the new representation in parallel, migrate the ~64 position-read sites in graded passes, and after each pass run `node equiv_harness.js check` — any differing result is a regression, surfaced immediately.

How it works: a seedable PRNG (mulberry32) replaces `Math.random` (all engine randomness flows through `d6`/`d3`), so identical inputs give identical outputs. A battery of fixed scenarios exercises combat math, the two-step Fight phase, Pile In / Consolidate movement, objective control, Stealth, Lone Operative, the ability-fx target-keyword join, and the full wound-table branch set; each emits a compact position/count/outcome fingerprint. `record` writes the baseline; `check` re-runs and diffs, exiting non-zero on any change.

Validated two ways before being trusted: (1) it is deterministic — green twice in a row against the unchanged engine; (2) it actually catches regressions — a deliberately injected `woundTarget(s==t)` off-by-one was flagged on the exact scenario, then cleared on revert. The first draft of the battery *missed* that injected bug (no scenario hit the equal-Strength branch), which surfaced a coverage gap; a dedicated wound-table branch-coverage scenario was added to close it. The Drukhari-Empowered scenario gracefully skips when the export's real templates aren't loaded, and pins the live Pain-ability join when they are.

Rebuild plan this enables (parallel, derived-token, prove-before-flip): make per-model positions canonical with the token-level `hx`/`hy` becoming a derived centroid (so unmigrated sites keep working), migrate sites in tested passes against this oracle, then build the stronger AI and the mass-sim on the single clean model — no dual-representation fallback to carry or later tear down. Per-model *positions* the rewrite can synthesise and own; per-model *wargear loadouts* stay blocked until a data source carries them, but the engine becomes ready for them.


### Core Rules — weapon-ability gap fill (Heavy, Melta, Blast, Indirect Fire)
An audit of `resolveAttacks` against the Core Rules weapon-ability list found the pipeline already handled Anti-X, Sustained Hits, Lethal Hits, Devastating Wounds, Twin-linked, Torrent, Pistol, Rapid Fire, Assault, Lance, Precision, Hazardous and Ignores Cover. Four genuine gaps were filled, each additive and gated behind its `ab.*` flag so existing combat is untouched:
- **[BLAST]** — +1 attack per 5 models in the target unit (`+floor(target.models/5)` per attacking model). Verified: 2 models vs a 15-model unit = 8 attacks.
- **[MELTA X]** — +X Damage when the target is within half range. Verified: more damage dealt at 4" than at long range with identical dice.
- **[HEAVY]** — +1 to Hit if the bearer's unit Remained Stationary this turn. Verified at BS6+: stationary scored 11 hits to moving's 6 on identical dice.
- **[INDIRECT FIRE]** — if the attacker has no line of sight to the target, the attack still resolves at −1 to Hit and the target gains the Benefit of Cover. Verified: fewer hits without LoS than with.

This work was done under the equivalence harness: the oracle stayed green across all pre-existing scenarios (proving the additions changed nothing that worked before), and four new scenarios were added and recorded to pin the new behaviour. The harness also caught a too-weak first draft of the Heavy test (equal hit counts that didn't actually demonstrate the bonus), which was tightened to a BS6+ case where the +1 is unmissable. The full battery is now 15 scenarios; an 80-step game with Blast/Melta weapons in the pipeline runs clean.

Remaining Core Rules not yet modelled are the structural/system ones rather than combat-math: the Reserves/Deep Strike deployment system, transports/embark + Firing Deck, aircraft/Hover, and Deadly Demise magnitude (blocked on data) — each tracked elsewhere in this README as dormant.


### Per-model rebuild — Pass 1 (the migration seam)
First concrete step of the per-model rewrite, done under the equivalence oracle. This pass is pure scaffolding: it changes NO behaviour (all 15 oracle scenarios byte-identical green), but installs the seam every later pass will use.

Added to `newUnit`: a dormant `modelPos` field (null). Added two accessors:
- `positionsOf(u)` → array of model positions; `[{hx,hy}]` derived from the token while `modelPos` is null, the real array once populated.
- `unitAnchor(u)` → the canonical single position; the token `hx/hy` while null, the centroid of `modelPos` once populated.

First consumer migrated: `dist(a,b)` now measures between `unitAnchor(a)` and `unitAnchor(b)` instead of raw token fields. Because `modelPos` is null everywhere, this is transparent today — but it means distance is already centroid-aware the moment models get positions.

Verified two ways: (1) the oracle is byte-identical green, proving existing behaviour is untouched; (2) a direct seam test confirmed the accessors engage when `modelPos` is populated — `unitAnchor` returns the true centroid (and tracks it as models move), `positionsOf` returns all models, and `dist` measures from the centroid. So the layer is transparent now and live the instant data populates it.

Migration plan from here (each pass proven green before the next): populate `modelPos` at deployment as a cluster around the drop point; make the token `hx/hy` a derived view so unmigrated readers stay correct; migrate remaining position consumers (engagement, visibility, movement, objective range) one at a time through the accessors; then light up the capabilities per-model positions unlock — Unit Coherency, multi-target melee, Blast-vs-clustered-models, base-to-base. Per-model *positions* the rebuild synthesises and owns; per-model *wargear loadouts* stay blocked until a data source carries them, but the structure becomes ready for them.


### Per-model rebuild — Pass 2 (populate modelPos, keep it synced)
Second pass, still behaviour-neutral (all 15 oracle scenarios byte-identical green). Units now carry real per-model positions; the token stays authoritative for now and modelPos tracks it.

`syncModelPos(u)` builds a symmetric cluster of `u.models` positions centred on the token (anchor cell first, then a ring of ±1/±2 offsets). Because the cluster is symmetric its centroid equals the token exactly, so `unitAnchor()` — and every consumer routed through it — is unchanged. It is called at every lifecycle point that sets or changes position: deployment, reinforcement arrival, Normal/Fall Back/Advance moves, charge (`moveAdjacent`), Pile In / Consolidate (`stepToward` and its undo paths), the AI objective move, and after damage (so the array shrinks with casualties). Going into Reserves clears `modelPos` to null.

Verified: (1) oracle byte-identical green — the symmetric-cluster centroid keeps `unitAnchor` pinned to the token; (2) a direct test showed modelPos populating at deploy (5 positions, centroid on token), tracking a move (centroid follows to the new cell), and shrinking with casualties (5→2 in lockstep with `models`); (3) an 80-step full game ran clean with a post-game integrity check finding zero modelPos/model-count desync across all phases.

State after Pass 2: the per-model representation is live and self-maintaining, but no consumer reads it for behaviour yet — the token remains the source of truth. Next passes: invert authority (modelPos becomes canonical, token becomes the derived centroid) once the remaining position consumers (engagement, visibility, objective range) read through the accessors; then enable the capabilities per-model positions unlock (Unit Coherency, multi-target melee, Blast-vs-clustered-models, base-to-base).


### Per-model rebuild — Pass 3 (migrate the measurement consumers)
Third pass, still behaviour-neutral (15/15 oracle byte-identical green). Every position *measurement* now reads through the accessors, so no measurement consumer cares anymore whether the token or modelPos is authoritative — the precondition for the authority-inversion pass.

Migrated to read `unitAnchor`/`positionsOf`: `visible` (line-of-sight), `engaged` and `pathThroughOpenHatch` (engagement range — so `inER` and every Fight/AI caller inherit it), `unitOnObjective`, both objective-control scoring loops, `hasCover` (objective + barricade reads), and the faction sticky-objective check. Deliberately left token-based for now: the raw `Math.hypot(u.hx-…)` reads *inside movement functions* that test a candidate destination cell — those are about where a unit is trying to move to, not where its footprint is, and they belong to the authority-inversion pass which handles movement holistically.

Verified beyond transparency: with an asymmetric `modelPos` whose centroid is displaced from the token, `engaged` correctly flips to true when the centroid (not the token) is in range, and `unitOnObjective` follows the centroid too — proving the consumers genuinely read the per-model footprint, not merely fall through to the token. Oracle byte-identical green (Pass-2 clusters are symmetric, so centroid==token in normal play); 80-step full game clean with zero modelPos desync.

Next: authority inversion — make `modelPos` canonical and the token a derived centroid, then migrate the movement candidate-cell reads as part of that holistic change. After that, the capabilities per-model positions unlock (Unit Coherency, multi-target melee, Blast-vs-clustered-models, base-to-base).


### Per-model rebuild — Pass 4 (authority inversion)
The pivotal pass: `modelPos` is now the **canonical** store and the token `hx/hy` is a **derived centroid** of it — the inverse of the original single-token model. Still behaviour-neutral (15/15 oracle byte-identical green), because `syncModelPos` keeps building a symmetric cluster around the requested anchor, so the recomputed centroid lands exactly on the integer cell callers set.

Added `recenterToken(u)` (token = rounded centroid of modelPos; called at the end of `syncModelPos` and after any per-model move) and `moveModel(u,i,hx,hy)` — the per-model move primitive the capability layer will build on. `unitAnchor` still returns the exact float centroid for measurement; the rounded token is for legacy raw reads and rendering.

Why inversion was safe to do now and not earlier: Passes 1–3 routed every position *measurement* through `unitAnchor`/`positionsOf`, so by this pass nothing downstream cared which representation was authoritative — flipping the source of truth changed no observable behaviour. Verified beyond transparency: moving all models via `moveModel` correctly drags the derived token with them; flinging a single model asymmetrically pulls the centroid partway (token 15→17 with one model flung to 23 while others stay ~15) — the divergence that was structurally impossible under the single-token model. 80-step full game clean, zero integrity failures (modelPos length == model count, token always a valid integer cell).

State: the engine now genuinely represents units as individual model tokens with a derived anchor, and has a primitive to move models independently. Remaining for the capability layer (each proven green, each likely adding oracle scenarios): Unit Coherency enforcement, multi-target melee (a unit splitting attacks across several engaged enemies), Blast counting clustered models, and base-to-base contact. Per-model *wargear loadouts* remain blocked on data, but the structure is now ready to carry them (a per-model `loadout[]` parallel to `modelPos`).


### Per-model rebuild — Pass 5 (capability layer: Unit Coherency)
First pass of the capability layer — the first that *uses* per-model positions to add behaviour rather than preserve it. Authored against the Core Rules coherency text: a unit of 2-6 models must have every model within 2" of at least one other; 7+ within 2" of at least two; at end of turn, models out of coherency are removed one at a time (counting as destroyed but triggering no on-death rules).

On this board 1 cell = 2", and `syncModelPos` builds a contiguous ~2-cell-radius cluster, so coherency is modelled as a connectivity test (`COH_CELLS=2`): a unit is coherent if every model has the required number of links (1, or 2 for 7+ models) AND all models form one connected group (flood-fill). A freshly-synced cluster always passes, which is why this is behaviour-neutral in normal play and the prior 15 oracle scenarios stayed green. Incoherency only arises once individual models are displaced via `moveModel` — exactly when the rule should apply.

Added `inCoherency(u)`, `enforceCoherency(u)` (removes the most-isolated model repeatedly until coherent; shrinks `modelPos`/`models` directly, no death-trigger chain, per the rule), and an `endTurn` hook that runs it on the ending player's units. A new oracle scenario pins the behaviour: synced cluster coherent (true), stranding a model breaks it (false), end-of-turn removes exactly the one stranded model (5→4), unit coherent again after, and an 8-model cluster satisfies the two-link threshold. 16 scenarios total; 80-step game clean with coherency active and zero model/position desync.

NOTE on harness hygiene: inserting the scenario initially clobbered the tail of `weapon_indirect` (a str_replace boundary slip); caught immediately by a syntax error, the indirect scenario was restored and both verified before recording. Next capability passes: multi-target melee (a unit splitting attacks across several engaged enemies, now that models have distinct positions), Blast counting clustered models, base-to-base contact.


### Per-model rebuild — Pass 6 (capability layer: multi-target melee)
Second capability pass: a melee unit now splits its attacks across the enemies its models are individually engaged with, instead of dogpiling the single closest foe. This is the most direct payoff of per-model positions — a unit straddling two enemies fights both.

`meleeTargetSplit(u)` partitions the unit's models by which enemy each is engaged with (per-model position vs enemy anchor, same ENGAGE_IN reach, hatch-aware), returning `[{enemy,nModels}]`; the Fight loop resolves `resolveAttacks` once per partition with that enemy's share of models. Verified: a 4-model unit placed two-left/two-right between two foes splits 2/2 across both targets, all models accounted for.

Two design corrections during the pass, both caught by the oracle and worth recording:
1. **Null-modelPos regression.** The first version assumed `modelPos` was always populated; units positioned without `syncModelPos` (including the oracle's fight scenarios) have null `modelPos`, so `positionsOf` returned a single fallback position and the unit attacked as if it had ONE model — silently dropping attacks. The oracle flagged `fight_fightsfirst` and `fight_consolidate_chain` immediately. Fix: when `modelPos` is absent or its length ≠ model count, fall back to the exact old behaviour (whole unit vs closest engaged enemy), making the capability strictly additive.
2. **Centroid-gated foes.** The enemy-target gate initially used unit-level `engaged()` (centroid-based), but the whole point of multi-target is that edge models reach enemies the centroid does not. Fix: when positions exist, an enemy is a valid target if ANY model is within reach of it.

After both fixes: 17 scenarios, all green — the two fight scenarios restored to their correct baselines (additive fallback) and the new `multitarget_melee` scenario pinning the 2/2 split. 80-step game clean, zero desync. Next capability passes: Blast counting clustered models (already counts target models; could refine to footprint), base-to-base contact refinement.


### Outstanding: per-model VISUALIZATION (rendering, not engine)
The per-model rebuild (Passes 1–6) is complete at the *engine/logic* level: units are represented as individual model tokens (`modelPos`), and coherency, multi-target melee, movement and measurement all use those positions. However the *renderer* still draws ONE marker per unit at the token/centroid — the individual models are not yet visualized as multiples on the board. This is a rendering task, independent of the engine work, and is parked as an outstanding item to pick up after the structural systems (it does not block them, and the equivalence oracle does not cover rendering).


### Structural systems — Reserves / Deep Strike (unified across all modes)
First structural system, built against the pasted Core/Boarding rules text and applied to ALL modes (open, boarding, sandbox), since every mode defines Entry Zones.

Rules encoded:
- **Deep Strike placement** stays the >9"-from-enemies rule, but the horizontal distance measurement now IGNORES Walls and closed Hatchways: an enemy whose straight line to the drop point is blocked by a wall/closed hatch does not constrain placement (`!segBlocked` guard). No-op in open mode (no walls); meaningful in boarding.
- **Arrival gating** (`beginReinforcements`): Strategic Reserves may arrive in an Entry Zone with no models of that player from **round 1**, one per empty zone (`emptyEntryZonesFor`); Deep Strike units may instead be set up via their rule from **round 2**, one per turn. The reinforcement action now carries `zoneSlots`/`dsEligible` so the UI/AI know what is available.
- **End-of-round-3 destruction**: any unit still in Reserves at the end of round 3 counts as destroyed — EXCEPT units placed into Reserves after round 1 started. Tracked via a new `_reserveRound` stamp (0 = pre-battle; set to the current round by `reserveUnit`; Cult-Ambush resurrections stamped with the current round so they are carve-out protected).

Verified by a new oracle scenario (`reserves_deepstrike`): near-placement rejected, far accepted, placement behind a wall accepted (wall-ignored distance), Deep Strike blocked in R1 and allowed in R2, and the end-of-R3 carve-out (pre-battle reserve destroyed, R3-reserved survives). 18 scenarios total, all green; 80-step full game clean.

**Known gap (belongs to the AI work, not this system):** the AI does not yet use the reinforcement arrival path, so an AI player holding reserves loses them to the end-of-R3 rule. The rules system is correct; AI reinforcement behaviour is unbuilt and is deferred to the AI portion. Next structural systems: Transports/embark + Firing Deck, Aircraft/Hover, Deadly Demise (mechanism only — per-unit magnitude is data-blocked).


### Structural systems — Transports (embark / disembark / Firing Deck / destroyed)
Second structural system, built against the pasted Transports rules text.

Fetch extension (no Netlify change needed — the source CSV already carries it): the importer now reads **transport capacity** from Wahapedia's Datasheets `transport` field (parses the leading number) into `transportCapacity`, and **Firing Deck x** from the captured abilities into `firingDeck`. `newUnit` carries `_capacity`, `_firingDeck`, and embark state (`_embarkedIn`, `_disembarkedThisTurn`). So a re-fetched factions-data.json populates real values automatically.

Mechanics encoded:
- **Embark**: a unit ending within 3" of a friendly TRANSPORT embarks (off-board, `_embarkedIn`, excluded from combat/targeting/scoring); blocked if it disembarked this phase or the transport is at capacity (`transportHasRoom`; unknown capacity allowed-but-flagged pending data).
- **Disembark**: set up wholly within 3" of the transport, clear of Engagement Range; cannot disembark from a transport that Advanced or Fell Back; if the transport already made a Normal move, the unit counts as moved and cannot charge.
- **Firing Deck x**: the transport gains up to x ranged weapons sourced from embarked units, and those units are locked out of shooting this phase (`firingDeckShots`).
- **Destroyed transport**: embarked units immediately disembark before removal; D6 per model (1 → 1 mortal to the unit), unit Battle-shocked, counts as moved, cannot charge. **Emergency Disembarkation** widens placement to 6", mortal on 1-3, and destroys any model that still cannot be placed. Wired at both death sites (applyDamage/applyMortals), before the on-death trigger chain.

Verified by a new oracle scenario (`transports`): capacity respected (5 fits, +8 rejected at cap 10), embarked unit off-board, disembark within 3", Firing Deck grants a shot and locks the passenger out, and a destroyed transport forces the passenger to bail out Battle-shocked. 19 scenarios, all green; 80-step game clean.

**Data note:** capacity and Firing Deck values are now *consumed* correctly; real per-datasheet values arrive when the re-fetched export carries them (the extraction is in place). Per-model weapon selection for Firing Deck is modelled as an effect (extra shots + passenger lockout) rather than exact per-model loadouts, consistent with the per-model-loadout data gap. Next structural systems: Aircraft/Hover, then Deadly Demise (mechanism; magnitude data-dependent).


### Firing Deck completed + the unseeable-X override library (xvalues.js)
Completing Firing Deck surfaced the same data limitation as Deadly Demise: the export carries only the generic glossary text for these abilities (the literal placeholder 'x'), never the per-datasheet number. The re-fetched factions-data.json DOES now carry real `transportCapacity` (verified: Rhino 12, etc., across 178 transports) and the `firingDeck` flag — but not the Firing Deck `x` value, nor Deadly Demise magnitude, nor Scouts distance.

Resolution (two parts):
1. **Default firingDeck to 2 when present.** The importer now sets `firingDeck:2` for any transport whose abilities mention Firing Deck without a parseable number (2 is the modal 10e value, correct for the large majority). Verified end-to-end against the real export: all 60 Firing Deck transports now have a working value.
2. **`xvalues.js` — a re-fetch-surviving override library** (new file, loaded before app.js like ability_fx.js). Keyed by datasheet_id → `{firingDeck, deadlyDemise, scouts}`. It ships pre-populated with firingDeck:2 for all 60 Firing Deck transports and `null` placeholders for 721 Deadly Demise + 110 Scouts datasheets. `applyXValues()` runs after every import (both the saved-bundle and CSV paths), stamping real values over the imported templates; a `null` leaves the engine's default/dormant handling in place. Because it is a separate hand-maintained file, manually entered X's are never wiped when a fresh factions-data.json is dropped in — the future workflow you specified.

The file header doubles as the **worklist**: 60 Firing Deck (defaulted), 721 Deadly Demise, 110 Scouts entries to fill in over time. Values accept a number (firingDeck:2, scouts:6) or a dice string for Deadly Demise ('D3','D6'); the engine rolls dice strings. Verified by oracle scenario `xvalues_override` (override beats imported value; a non-null Deadly Demise stamps as 'D3') and an end-to-end real-export load (Chimera firingDeck 2, Rhino capacity 12, 60 transports firingDeck≥2). 20 scenarios green.

DEPLOY NOTE: `xvalues.js` is a NEW file — add it to the repo (GitHub → Add file → Upload files) alongside the changed app.js and index.html (which now loads it).


### Wargear options → loadout synthesis (fetch + parser)
Extended the data pipeline to fetch Wahapedia's **Datasheets_options** table — the wargear-OPTIONS data (free-form legal-swap rules), distinct from Datasheets_wargear (the flat weapon-profile menu the engine already had). Added it to the Netlify whitelist and to both importer fetch-lists.

Each template now carries `loadout:{options:[...],unparsed:[...]}`:
- **Raw text is attached losslessly** to every datasheet (nothing dropped).
- A **parser** (`parseLoadoutOptions`) converts the COMMON 10e grammar into structured options — `{ratio:{n,per}|null, who, replace:[], with:[], take:[], raw}` — covering: "N in M models may replace A with B", "For every M models, 1 may replace A with B", "the Sergeant/Exarch/Champion may take/replace …", "this model may replace A with one of B, C", "any model may take B".
- Phrasings it can't confidently handle (conditional "if this unit contains 10 models…", nested clauses) are **kept verbatim and flagged `unparsed`** — illegal loadouts are never invented.

Verified by a parser unit-test and a pinned oracle scenario (`loadout_parser`): ratio/who/replace/with/take extracted correctly, "one of the following:" prefixes stripped, conditional lines correctly left unparsed. 21 scenarios green.

**IMPORTANT — needs a re-fetch to populate:** the currently-loaded factions-data.json predates this change and has NO options data, so `loadout` is empty for now. To activate: re-fetch (the new whitelist pulls Datasheets_options) and upload the new factions-data.json. Then real parse coverage can be measured and verified under the oracle. DEPLOY: changed files are app.js and netlify/functions/wahapedia.js (plus harness/baseline). The Netlify function change requires a redeploy for the new CSV to be fetchable.


### Loadout parser — validated against the real export, rewritten to the actual grammar
The re-fetched factions-data.json (built by the new importer) carried options text for 1319 datasheets / 2804 lines. The first run was the honest reckoning: **0% parsed** — the parser had been written against an ASSUMED grammar ("may replace its X with Y") while the real Wahapedia phrasing is different ("This model's X can be replaced with 1 Y", "can be equipped with", "Any number of models can each have their X replaced with Y", "For every N models…"). The earlier unit-test had passed only because its inputs were written in the assumed grammar — a real testing-against-assumptions miss that the re-fetch exposed.

Rewrote the parser against the actual grammar (verified by bucketing all 2804 real lines). Coverage now **89% of real option lines** (2243 / 2513), with 291 "None" placeholders correctly skipped (no options) and footnote restriction lines ("* That model's X cannot be replaced") excluded. The remaining ~11% is genuine long tail — token/upgrade grants ("…can have 1 Aspect Shrine token"), and rare nested phrasings ("Each model can have each X it is equipped with replaced…") — kept as raw text and flagged unparsed. No illegal loadout is ever invented.

Parsed options are structured as `{ratio:{n,per}|null, who, replace:[], with:[], take:[], raw}`: ratio `*`/N for "any number"/"up to N", named subjects ("Skitarii Ranger Alpha"), and multi-choice "one of the following:" lists split correctly (including the newline-joined, count-prefixed real format). The oracle scenario `loadout_parser` is now pinned against REAL Wahapedia strings (replace/equip/any-number/up-to-N parse; None and footnotes skipped). 21 scenarios green.

This unblocks the structure for per-model loadouts; wiring these options into actual per-model wargear assignment (and thus exact Firing Deck weapon selection and composition-dependent ability riders) is the next consumer step, now that the data exists and parses.


### Loadout parser — coverage raised to 93%
After inspecting the unparsed remainder, several were recoverable patterns the parser had narrowly missed, not true long tail: "can each BE EQUIPPED WITH" (vs "have…replaced"), active-voice "can each replace their A with B", "Up to two/three" word-numbers, "For every N models, up to M <named> can each have…", and colon-joined option lists ("equipped with:1 X"). Added these patterns plus a wordNum helper. Coverage rose 89% → **93%** of real option lines (2325/2513); datasheets with any unparsed line fell 112 → 93.

The genuinely-remaining tail (~134 lines across 93 datasheets) is dominated by conditional branches ("If this unit contains 9 or fewer models: …"), multi-item bundles ("A and 1 B and 1 C"), and a nested per-weapon case — phrasings where forcing more regex would risk mis-parsing, so they are kept verbatim and flagged unparsed (never invented). The full datasheet list is available via the parser's unparsed flags. Oracle scenario `loadout_parser` (pinned to real Wahapedia strings) stays green; 21 scenarios total.


### Loadout parser — generalized to ~100% of tractable options
Pushed coverage from 93% to **99.7%** (2415 parsed / 7 unparsed) after the user rightly pushed back that the affected units (Plague Marines, Chosen, Death Korps, Crisis Battlesuits, the Aspect squads) are the core of 10e. Investigation confirmed the gap was mostly the PARSER being narrow, not the data being hard.

Replaced the one-pattern-at-a-time approach with a **generalized two-stage parser**: Stage 1 peels off a quantifier prefix (any number / all (of the) models / all <Named> in this unit / up to N / N / "for every N, up to M <Named>" / bare "this model"/"this unit") and records the ratio; Stage 2 splits on the action verb (can [each] be replaced / can [each] have … replaced / can [each] be equipped with / can [each] replace … with / must be equipped) and extracts replace/with/take. This collapsed dozens of subject·verb variants into one cross-product and caught the "All models in this unit can each have their A replaced with B" family that was the bulk of the miss (Necrons, Custodes, Reivers, Tyranids, etc.).

Also: conditional options ("If this unit contains 10 models, …", "If this model is equipped with X, …") are now parsed with the condition preserved on the option (34 captured) rather than dropped; named models' FIXED loadouts ("Cassius is equipped with: …") and token/attachment grants are correctly skipped as non-options; two source typos ("can replaced", "replaced one of the following", missing "can be") are normalized.

The irreducible **7 remaining** are genuinely structural, not parser gaps: multi-item bundle swaps ("replace boltgun AND melee weapon with 1 X and 1 Y"; "thermal cannon → 1 A and 1 B"), unit-attachment grants (Plasmacyte), and choice-of-actions ("can do one of the following: Replace…/Replace…"). These need a richer data model than replace→with; forcing a parse would risk an illegal loadout, so they are kept verbatim for the muster screen to present as raw text. Oracle scenario `loadout_parser` re-recorded (the token-grant line now correctly skipped rather than counted unparsed); 21 scenarios green.


### Plasmacyte (Necron Destroyer Cult) — modelled as an ability, not a loadout swap
The "For every 3 models in this unit, this unit can have 1 Plasmacyte" line was one of the 7 unparsed loadout lines, but it isn't a wargear swap — it's a unit-attachment-plus-combat-buff. Built it against the real ability text (verified in the export for both units that have it: Skorpekh Destroyers and Ophydian Destroyers): "Once per battle for each Plasmacyte this unit has, when this unit is selected to fight, … until the end of the phase, melee weapons equipped by models in this unit have [DEVASTATING WOUNDS]."

Implementation: `_plasmacytes` on each unit, granted `floor(models/3)` at battle start by `grantPlasmacytes` (the "can have" cap; the muster screen can later make it a choice). In the Fight phase, when a Plasmacyte-bearing unit is selected to fight and has a token, the engine spends one and flags the unit's melee weapons with `devastating` for that fight (the engine already resolves [DEVASTATING WOUNDS]: critical wounds become mortals bypassing saves), then restores the weapon template afterward so the buff doesn't leak. The parser now skips Plasmacyte lines as non-swap grants.

Verified: 6-model unit gets 2 tokens; spending one makes critical wounds produce mortal damage (12 devastating mortals in a high-volume test vs 0 without); no buff leak; and a full 80-step game spent both tokens automatically with no errors. Oracle scenario `plasmacyte` pins grant count, devastating effect, and no-leak. NOTE: the scenario was first written with too few attacks to roll a wound-crit (showing a false 0 devastating); corrected to high-volume so it exercises the real path. 22 scenarios green.

Loadout parser unparsed count drops from 7 to 5 (the two Plasmacyte lines are now handled).


### Loadout parser — 100% coverage (choice-of-actions + multi-item bundles)
Closed the final gap from the Assault Sergeant example. The remaining unparsed lines were two distinct structures, both now handled, taking coverage to a genuine **100.00% (2420/2420 option lines, 0 unparsed)**:

1. **Choice-of-actions** — "<subject> can do one of the following: Replace A and B with C. Be equipped with D." Now parsed into an option carrying a `choices` array, each entry a full sub-action ({replace,with,take}); the player picks one. Verified: the Assault Sergeant yields 2 choices — a two-weapon replace (bolt pistol + chainsword → twin lightning claws) and a shield equip.
2. **Multi-item bundles** — "X (and Y) can be replaced with: 1 A and 1 B". A correctness fix, not just coverage: the earlier splitter MANGLED these (breaking "1 plasma pistol and 1 chainsword" into separate wrong items — a false positive producing illegal loadouts). Now "and"-joined bundles are protected during splitting and kept as one option; `splitAnd` separates the multi-weapon replace side. Also handles the active-voice "can replace their A with:" and colon-join ("with:1 X") forms.

The `choices` field is new on parsed options (alongside ratio/who/replace/with/take/condition). Oracle scenario `loadout_parser` now asserts the choice-of-actions structure; 22 scenarios green. Every wargear option in the export is now structured data for the muster-screen consumer — multi-item bundles and per-model conditional swaps included.


### Aircraft facing subsystem — Pass A (dormant facing layer)
Building the facing subsystem the user asked for, using the same staged/de-risked method as the per-model rebuild: introduce the representation first as a dormant layer, prove it behaviour-neutral, then activate it for aircraft only in later passes.

Pass A adds a `facing` field to every unit (degrees, 0°=east, clockwise on the y-down grid; null = no facing tracked, which is every existing unit). Pure heading helpers added: `headingDeg` (angle between two points), `normDeg`/`pivotDiff` (smallest signed/abs angle, wrap-correct), `withinArc` (is a target within ±arc of facing), and `setFacingFromMove` (records heading as direction of travel). `setFacingFromMove` is now called at every move commit (Normal, Fall Back, Advance, charge moveAdjacent, stepToward) — so facing is recorded on every move — but NOTHING reads facing yet, so behaviour is byte-identical (22→23 scenarios all green; the rewrite touches no outcome).

Verified beyond the oracle: a direct test confirmed correct headings (E/S/W), pivot wrap (170°→-170° = 20°), the forward-arc test (ahead/behind/inclusive-at-90°), travel-direction recording, and heading-preservation on a zero-displacement move. Pinned by scenario `facing_math`.

Remaining passes: B = aircraft movement consequences + facing-constrained ≥20" straight move and 90° pivot (activates facing for AIRCRAFT only); C = aircraft charge/fight/targeting (FLY-only) + Hover mode; D = renderer heading indicator.


### Aircraft facing subsystem — Pass B (facing-constrained aircraft movement)
Activates the Pass-A facing layer for AIRCRAFT only (capability work — outcomes change for aircraft, pinned by new scenarios). Helpers `isAircraft` (AIRCRAFT keyword AND not in Hover) and `unitCanFly` added.

`tryMove` now branches for aircraft: only a Normal move; the destination must be at least 20" away (AIRCRAFT_MIN_MOVE) AND within the forward ±90° arc of current heading (modelling "move straight forward, then pivot up to 90°" — a point behind would need a turn >90° before moving). No Advance/Fall Back/Remain Stationary. The AI movement path mirrors this: an AI aircraft makes the best legal >=20" forward pass toward the nearest enemy, or is placed into Strategic Reserves (returns next turn) if no legal pass exists (edge of board). `reserveAircraft` puts every non-Hover AIRCRAFT into Strategic Reserves at battle start, per the deploy rule; they arrive via the existing reinforcement path.

ABSTRACTION NOTE: true straight-line-then-pivot geometry is modelled via the heading layer (destination within ±90° arc + 20" minimum + heading updated to travel direction). On this grid, with no firing arcs or rear armour, this captures every gameplay consequence of the rule; exact base-orientation between pivots is not simulated.

Verified by scenario `aircraft_move`: sub-20" rejected, >=20" forward accepted, rearward (>90° pivot) rejected, non-aircraft unaffected, and battle-start reserve placement. 24 scenarios green; 80-step game with an aircraft ran clean (jet correctly started in Reserves). KNOWN: AI doesn't yet bring reserves back on (the documented AI-reinforcement gap), so an AI aircraft currently stays reserved — rules are correct, AI behaviour is deferred to the AI work.

Remaining: Pass C = aircraft charge/fight/targeting (FLY-only, can't charge/pile-in) + Hover mode toggle; Pass D = renderer heading indicator.


### Aircraft facing subsystem — Pass C (combat rules + Hover)
The combat-side aircraft rules (clean keyword-gated checks) plus Hover mode.

- **Aircraft cannot charge** — `beginChargeSelection` refuses for `isAircraft` units.
- **Only FLY may charge an aircraft** — charge target filter excludes AIRCRAFT enemies unless the charger can FLY.
- **Aircraft melee only FLY, and only FLY may melee aircraft** — `meleeTargetSplit` foe-filter drops any (attacker,enemy) pairing where one side is an aircraft and the other can't FLY. This also yields the eligibility rule for free: an aircraft with no FLY enemy in range gets an empty target split (makes no attacks), and a non-FLY unit engaged only with an aircraft likewise has no legal target.
- Pile-in/Consolidation already ignore AIRCRAFT for non-FLY models (`closestEnemyTo`).
- **Hover mode** (`setHover`): for AIRCRAFT with a Hover ability, sets `_hover` (so `isAircraft` returns false — every aircraft special-case is bypassed) and Move:=20"; reversible (restores original Move). `unitHasHover` detects the ability.

Verified by scenario `aircraft_combat`: aircraft can't charge; non-FLY can't target an aircraft with a charge; an aircraft engaged with both ground and flyer hits only the flyer; a non-FLY unit engaged only with an aircraft has no legal melee target; Hover strips aircraft status and sets M=20, reversibly. 25 scenarios green; 80-step game with aircraft + flyers ran clean.

Remaining: Pass D = renderer heading indicator (draw facing + the 90°/20" reach), so the constraints are visible and verifiable on screen. The Hover toggle and aircraft reserve-arrival will get UI affordances alongside the renderer work.


### Aircraft facing subsystem — Pass D (renderer) + movement-cost-around-walls
Three rendering/interaction features plus a pathfinding upgrade:

- **Facing indicator**: every unit that tracks a heading now draws a prow line + arrowhead in its facing direction (gold for AIRCRAFT, grey otherwise), so the facing the engine tracks is visible.
- **Translucent move-preview on cursor hover**: when a movable unit is selected, hovering a cell shows a ghost token at that cell, a travel line (green = legal, red = illegal), a distance label, and a **heading-toward-cursor arrow** showing the direction the unit would face after moving. Driven by a mousemove listener tracking the hover cell (`hoverHx/hoverHy`); pure render, no engine state.
- **Movement cost through/around walls**: ground moves now route AROUND walls and pay the longer path distance instead of being hard-blocked. New `pathInches`/`pathCostCells` helpers run a Dijkstra over the cell grid (8-connected, diagonal = √2) where a step is allowed only if the segment between cell centres isn't wall-blocked; a corner-cut guard prevents diagonals from squeezing through wall corners. `tryMove` uses this path distance as the move cost for non-aircraft (aircraft fly straight over terrain). Unreachable destinations are blocked with a clear hint. When there are no walls, behaviour is unchanged (straight-line distance), so the 25 oracle scenarios stay green.

Verified: a wall forces a longer detour (e.g. 8″ straight → 16.5″ around a 6-cell wall), a sealed cell is correctly unreachable (Infinity), open moves are unchanged, and `render()` runs clean headless with facing + preview in all states. 25 scenarios green.

STILL PENDING (needs user confirmation): finer GRID RESOLUTION. HEX_INCH=2 (1 cell = 2") is the SIMULATION resolution; CELL px is display only and already adaptive. Making the sim grid finer (e.g. HEX_INCH=1) doubles GW/GH and rescales every distance — a real change requiring full oracle re-record. Deferred pending confirmation of whether the user wants a finer simulation grid (smoother snapping) or just smaller on-screen cells.


### Finer simulation grid (HEX_INCH 2 -> 1)
The user confirmed they meant the SIMULATION grid was coarse (2" per cell), not the on-screen cell size. Doubled the resolution: `HEX_INCH` is now 1 (1 cell = 1 inch), so movement and positioning snap at 1" granularity instead of 2".

Done as a faithful refactor, not a rewrite: most distance code already converts via `*HEX_INCH` / `/HEX_INCH` and scales automatically. The only resolution-DEPENDENT constants (hardcoded cell counts assuming 2"/cell) were made to derive from the resolution via a factor `_RES=2/HEX_INCH`, so they keep the same PHYSICAL size at any resolution: board dimensions `GW/GH` (now 48x44, was 24x22) and the sandbox clamps, `COH_CELLS` (coherency stays 2 physical inches), and `_CLUSTER_OFFSETS` (per-model cluster keeps its physical footprint). `ENGAGE_IN` was already in inches (resolution-independent). Because every physical distance is preserved, the engine plays an identical game on a finer grid.

Validation discipline: re-recorded the oracle scenario-by-scenario. Exactly two scenarios changed — `aircraft_move` and `reserves_deepstrike` — both because their SETUPS hardcoded cell coordinates that meant different inch-distances on the new grid (e.g. an 11-cell move was 22" at 2"/cell, now 11"). These were test artifacts, not regressions; coordinates were updated to the new scale and re-verified to pass for the right reason. (Caught and corrected a near-miss where stale assertions would have recorded a wrong-reason green.) Functional proof: an M=6 unit now moves up to 6 cells (=6"), 7 cells (7") is not a plain Normal move; a full game on the 48x44 board ran clean in ~86ms (4x the cells, no performance issue). New invariant scenario `grid_resolution` pins HEX_INCH, board size, move granularity, and coherency. 26 scenarios green.

This completes the four Pass-D items (facing indicator, translucent move-preview with cursor-heading, movement-cost-around-walls, finer grid) and the full Aircraft/Hover facing subsystem (Passes A-D).